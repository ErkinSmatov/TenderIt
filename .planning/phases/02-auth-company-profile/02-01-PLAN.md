---
phase: 02-auth-company-profile
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/.env.example
  - backend/pyproject.toml
  - backend/app/models/__init__.py
  - backend/app/models/user.py
  - backend/app/models/company_profile.py
  - backend/app/db.py
  - backend/alembic/versions/0001_create_users_company_profiles.py
  - backend/tests/conftest.py
  - backend/tests/test_bin_validation.py
  - backend/app/services/__init__.py
  - backend/app/services/bin_validator.py
  - backend/pytest.ini
  - frontend/package.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "Backend installs PyJWT, pwdlib[argon2], redis, slowapi, resend without conflicts"
    - "Frontend installs jose and @hookform/resolvers without conflicts"
    - "JWT_SECRET, RESEND_API_KEY, FRONTEND_URL are settable via env"
    - "User and CompanyProfile tables exist in Postgres after migration"
    - "BIN checksum validator returns True for valid BIN and False for invalid format"
    - "pytest discovers async tests with asyncio_mode=auto"
  artifacts:
    - path: "backend/app/models/user.py"
      provides: "User SQLAlchemy model"
      contains: "class User"
    - path: "backend/app/models/company_profile.py"
      provides: "CompanyProfile SQLAlchemy model"
      contains: "class CompanyProfile"
    - path: "backend/app/models/__init__.py"
      provides: "Model registry for Alembic autogenerate"
      contains: "from app.models.user import User"
    - path: "backend/app/services/bin_validator.py"
      provides: "validate_bin function with checksum + position-5 check"
      contains: "def validate_bin"
    - path: "backend/alembic/versions/0001_create_users_company_profiles.py"
      provides: "Initial Alembic migration creating users and company_profiles tables"
    - path: "backend/pytest.ini"
      provides: "asyncio_mode = auto"
    - path: "backend/.env.example"
      provides: "JWT_SECRET, RESEND_API_KEY, FRONTEND_URL placeholders"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "backend/alembic/env.py"
      via: "import app.models triggers User + CompanyProfile registration on Base.metadata"
      pattern: "from app.models.user import User"
    - from: "backend/app/config.py"
      to: "Settings env loading"
      via: "pydantic-settings reads .env"
      pattern: "jwt_secret|resend_api_key|frontend_url"
---

<objective>
Foundation wave for Phase 2. Adds new settings (JWT_SECRET, RESEND_API_KEY, FRONTEND_URL),
installs all backend + frontend auth dependencies, creates User and CompanyProfile
SQLAlchemy models with the correct async relationship loading, generates the Alembic
migration, scaffolds the test infrastructure (asyncio_mode=auto + conftest fixtures),
and ships the pure-function BIN checksum validator with unit tests.

Purpose: All subsequent vertical slices (login, refresh, reset, profile) require these
foundations. Without this wave, no other plan can succeed.
Output: Updated config, pyproject.toml, package.json, models/, migrations/, tests/, services/bin_validator.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-company-profile/02-RESEARCH.md
@.planning/phases/02-auth-company-profile/02-PATTERNS.md
@CLAUDE.md
@backend/app/config.py
@backend/app/db.py
@backend/alembic/env.py
@backend/tests/conftest.py
@backend/pyproject.toml
@frontend/package.json

<interfaces>
From backend/app/db.py:
- class Base(DeclarativeBase)
- engine = create_async_engine(settings.database_url, ...)
- AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)
- async def get_db()

From backend/app/config.py:
- class Settings(BaseSettings) with model_config = SettingsConfigDict(env_file=".env")
- Existing fields: database_url, redis_url, minio_*, debug, secret_key
- settings: Settings (module-level singleton)

Models to expose:
- User(id, email, hashed_password, is_active, is_verified, created_at, company_profile: relationship)
- CompanyProfile(id, user_id FK unique, bin, company_name, legal_address, updated_at, user: relationship)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Settings and .env.example; install backend + frontend dependencies</name>
  <files>
    backend/app/config.py,
    backend/.env.example,
    backend/pyproject.toml,
    frontend/package.json,
    backend/pytest.ini
  </files>
  <read_first>
    backend/app/config.py,
    backend/pyproject.toml,
    backend/.env.example,
    frontend/package.json,
    .planning/phases/02-auth-company-profile/02-RESEARCH.md (Standard Stack section)
  </read_first>
  <behavior>
    - Settings exposes jwt_secret: str (default "change-me-in-production"), resend_api_key: str (default ""), frontend_url: str (default "http://localhost:3000")
    - settings.jwt_secret reads from JWT_SECRET env var via pydantic-settings standard upper-case mapping
    - pyproject.toml [project.dependencies] includes pyjwt==2.13.0, pwdlib[argon2]==0.3.0, redis==8.0.0, slowapi==0.1.9, resend==2.30.1
    - frontend/package.json dependencies include jose@^6.2.3 and @hookform/resolvers@^5.4.0
    - backend/pytest.ini contains [pytest] asyncio_mode = auto and testpaths = tests
    - backend/.env.example contains JWT_SECRET=, RESEND_API_KEY=, FRONTEND_URL=http://localhost:3000 placeholders alongside any existing keys
  </behavior>
  <action>
    Extend the existing Settings class in backend/app/config.py with three new fields: jwt_secret (str, default "change-me-in-production"), resend_api_key (str, default ""), frontend_url (str, default "http://localhost:3000"). Keep the existing secret_key field intact (it is referenced elsewhere — do not delete) but use jwt_secret as the new canonical JWT signing key going forward (per RESEARCH.md Open Questions Q2).

    Append the three new vars to backend/.env.example with empty/default placeholders. Append a JWT_SECRET generation hint comment: # Generate with: openssl rand -hex 32.

    Edit backend/pyproject.toml [project] dependencies array to add (preserve existing entries): "pyjwt==2.13.0", "pwdlib[argon2]==0.3.0", "redis==8.0.0", "slowapi==0.1.9", "resend==2.30.1". Run `pip install -e ".[dev]"` from backend/.

    Create backend/pytest.ini with [pytest] section: asyncio_mode = auto, testpaths = tests, addopts = -ra.

    Edit frontend/package.json dependencies to add "jose": "^6.2.3" and "@hookform/resolvers": "^5.4.0". Run `npm install` from frontend/.

    No code blocks in this action description — exact identifiers, version pins, and target files are listed above; the executor reads RESEARCH.md Standard Stack for context.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.config import settings; assert hasattr(settings, 'jwt_secret') and hasattr(settings, 'resend_api_key') and hasattr(settings, 'frontend_url')" && python -c "import jwt, pwdlib, redis, slowapi, resend" && cd ../frontend && node -e "require('jose'); require('@hookform/resolvers/zod')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E 'jwt_secret|resend_api_key|frontend_url' backend/app/config.py | grep -v '^#' | wc -l` returns 3 or more
    - Source: `grep -E 'JWT_SECRET|RESEND_API_KEY|FRONTEND_URL' backend/.env.example | wc -l` returns 3 or more
    - Behavior: `cd backend && python -c "from app.config import settings; print(settings.jwt_secret, settings.frontend_url)"` prints both values without error
    - Behavior: `cd backend && python -c "import jwt, pwdlib, redis, slowapi, resend"` exits 0
    - Behavior: `cd frontend && node -e "require('jose')"` exits 0
    - Source: `grep -c 'asyncio_mode = auto' backend/pytest.ini` returns 1
  </acceptance_criteria>
  <done>
    All new Python and Node packages install, Settings exposes the three new fields populated from env, pytest.ini configures asyncio_mode=auto, .env.example documents the new variables.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create User and CompanyProfile models with package __init__ exports</name>
  <files>
    backend/app/models/__init__.py,
    backend/app/models/user.py,
    backend/app/models/company_profile.py
  </files>
  <read_first>
    backend/app/db.py,
    backend/app/models/__init__.py (if exists),
    backend/alembic/env.py,
    .planning/phases/02-auth-company-profile/02-PATTERNS.md (User + CompanyProfile sections),
    .planning/phases/02-auth-company-profile/02-RESEARCH.md (Pattern 2, Pitfall 1, Pitfall 4)
  </read_first>
  <behavior>
    - User has columns: id (PK int), email (String(255) unique nullable=False indexed), hashed_password (String(255) nullable=False), is_active (Boolean default=True), is_verified (Boolean default=False), created_at (server_default=func.now())
    - User has company_profile: relationship(back_populates="user", lazy="selectin", uselist=False) producing Optional[CompanyProfile]
    - CompanyProfile has columns: id (PK int), user_id (ForeignKey users.id unique nullable=False), bin (String(12) nullable), company_name (String(500) nullable), legal_address (String(1000) nullable), updated_at (onupdate=func.now())
    - CompanyProfile has user: relationship(back_populates="company_profile", lazy="selectin")
    - app/models/__init__.py imports User and CompanyProfile so Base.metadata sees them
  </behavior>
  <action>
    Create backend/app/models/user.py and backend/app/models/company_profile.py using the exact column and relationship definitions from PATTERNS.md "User and CompanyProfile" section. Every relationship() call MUST include lazy="selectin" — this is the fix for Pitfall 1 (MissingGreenlet). Use SQLAlchemy 2.x Mapped + mapped_column annotation style; import Base from app.db.

    Create backend/app/models/__init__.py with explicit imports: from app.models.user import User and from app.models.company_profile import CompanyProfile, plus __all__ = ["User", "CompanyProfile"]. This is the Pitfall 4 fix — Alembic autogenerate requires the model classes to be loaded into Base.metadata before env.py imports app.models.

    Use String(255) for email and hashed_password, String(12) for bin, String(500) for company_name, String(1000) for legal_address. created_at uses server_default=func.now(); updated_at on CompanyProfile uses onupdate=func.now() and server_default=func.now().
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models import User, CompanyProfile; from app.db import Base; assert 'users' in Base.metadata.tables and 'company_profiles' in Base.metadata.tables; assert User.__table__.c.email.unique; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'lazy="selectin"' backend/app/models/user.py` returns 1 or more
    - Source: `grep -c 'lazy="selectin"' backend/app/models/company_profile.py` returns 1 or more
    - Source: `grep -E 'from app.models.user import User' backend/app/models/__init__.py | wc -l` returns 1
    - Source: `grep -E 'from app.models.company_profile import CompanyProfile' backend/app/models/__init__.py | wc -l` returns 1
    - Behavior: `cd backend && python -c "from app.models import User, CompanyProfile; from app.db import Base; print(sorted(Base.metadata.tables.keys()))"` includes both 'users' and 'company_profiles'
    - Behavior: `cd backend && python -c "from app.models import User; assert User.__table__.c.email.unique is True"` exits 0
  </acceptance_criteria>
  <done>
    Both model files exist, models/__init__.py exports them, Base.metadata contains both tables, every relationship uses lazy="selectin".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate Alembic migration; create BIN validator service with unit tests; extend conftest</name>
  <files>
    backend/alembic/versions/0001_create_users_company_profiles.py,
    backend/app/services/__init__.py,
    backend/app/services/bin_validator.py,
    backend/tests/test_bin_validation.py,
    backend/tests/conftest.py
  </files>
  <read_first>
    backend/alembic/env.py,
    backend/tests/conftest.py,
    backend/tests/test_health.py,
    .planning/phases/02-auth-company-profile/02-RESEARCH.md (Pattern 4, Pitfall 5),
    .planning/phases/02-auth-company-profile/02-PATTERNS.md (BIN validation section, Test structure section)
  </read_first>
  <behavior>
    - validate_bin("000000000000") returns False (all zeros — checksum may match but position 5 is '0', not legal entity)
    - validate_bin("12345") returns False (wrong length)
    - validate_bin("12345678901a") returns False (non-digit)
    - validate_bin(bin) where position 5 (index 4) is not '4', '5', or '6' returns False
    - validate_bin(valid_kz_bin_with_correct_checksum_and_position5_in_456) returns True
    - validate_bin("190140100099") with intentionally wrong checksum returns False
    - alembic upgrade head creates 'users' and 'company_profiles' tables in Postgres
    - conftest.py provides a session-scoped async client fixture and a db_session fixture that yields an AsyncSession against the test database
  </behavior>
  <action>
    Implement backend/app/services/bin_validator.py with the pure validate_bin function from PATTERNS.md (which includes the position-5 fix per Pitfall 5). Function signature: validate_bin(bin_str: str) -> bool. Reject non-digit, wrong length, position 5 not in {'4','5','6'}, then run the two-cycle checksum algorithm (weights1, weights2, mod 11, special-case 10 -> second cycle -> 0).

    Create backend/app/services/__init__.py as empty package marker.

    Create backend/tests/test_bin_validation.py with at least seven unit tests covering: empty string, short, long, non-digit, wrong position 5, wrong checksum, and at least one known-good BIN. For the known-good case, use synthesized BIN "190540000017" or another value whose checksum the executor verifies in a Python REPL during implementation; document the chosen BIN in a code comment with the computed checksum digits.

    Extend backend/tests/conftest.py: keep the existing session-scoped client fixture; add a function-scoped async db_session fixture that opens AsyncSessionLocal and rolls back at teardown so tests do not leak state. Do not break the existing test_health fixture contract.

    Generate the Alembic migration: from backend/ run `alembic revision --autogenerate -m "create_users_company_profiles"`. Move/rename the generated file to backend/alembic/versions/0001_create_users_company_profiles.py. Verify the upgrade() function contains op.create_table('users', ...) and op.create_table('company_profiles', ...) — if empty, the models __init__ from Task 2 is the culprit; fix and regenerate. Then run `alembic upgrade head` against the dev Postgres.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_bin_validation.py -x -q && alembic upgrade head && python -c "from sqlalchemy import create_engine, inspect; from app.config import settings; e = create_engine(settings.database_url.replace('+asyncpg', '')); insp = inspect(e); assert 'users' in insp.get_table_names() and 'company_profiles' in insp.get_table_names()"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'def validate_bin' backend/app/services/bin_validator.py` returns 1
    - Source: `grep -E "bin_str\\[4\\] not in" backend/app/services/bin_validator.py | wc -l` returns 1 or more (position-5 check)
    - Behavior: `cd backend && pytest tests/test_bin_validation.py -x -q` exits 0 with 7 or more tests collected
    - Behavior: `cd backend && python -c "from app.services.bin_validator import validate_bin; assert validate_bin('12345') is False and validate_bin('') is False"` exits 0
    - Behavior: `cd backend && alembic upgrade head` exits 0
    - CLI: `cd backend && python -c "from sqlalchemy import create_engine, inspect; from app.config import settings; e = create_engine(settings.database_url.replace('+asyncpg', '')); insp = inspect(e); print(sorted(insp.get_table_names()))"` prints a list containing 'users' and 'company_profiles'
    - Source: `grep -c 'db_session' backend/tests/conftest.py` returns 1 or more
  </acceptance_criteria>
  <done>
    BIN validator service file exists with checksum + position-5 logic, 7+ unit tests pass, Alembic migration applied, both tables visible in Postgres, conftest provides db_session fixture without breaking existing tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | .env file readable only by app user; secrets never logged or committed |
| process → DB | SQLAlchemy parameterised queries; no raw SQL string concat |
| migration → DB | Alembic migration is the only path that mutates schema; idempotent via revision IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | backend/.env containing JWT_SECRET / RESEND_API_KEY | mitigate | .env is in .gitignore; .env.example holds placeholders only; secret_key default ("change-me-in-production") must be rotated via openssl rand -hex 32 before any deploy (documented in .env.example) |
| T-02-01-02 | Tampering | SQLAlchemy column types | mitigate | String length caps (email=255, bin=12) prevent oversized writes; unique constraint on User.email at DB level prevents enumeration race |
| T-02-01-03 | Spoofing | BIN validator weak — accepting individual IIN instead of legal-entity BIN | mitigate | Position-5 check rejects values not in {'4','5','6'} (Pitfall 5); unit tests assert rejection of IIN-shaped 12-digit inputs |
| T-02-01-04 | Denial of Service | Alembic migration accidentally dropping data | accept | Wave 0 is greenfield (tables do not exist yet); downgrade() never run in prod; rationale: low risk for new tables |
</threat_model>

<verification>
- All three tasks' automated checks pass
- `cd backend && pytest tests/ -x -q` exits 0 (includes existing test_health + new test_bin_validation)
- `cd backend && alembic current` shows revision 0001
- No relationship() definition in backend/app/models/ omits lazy="selectin": `grep -RE 'relationship\\(' backend/app/models/ | grep -v 'lazy="selectin"' | wc -l` returns 0
</verification>

<success_criteria>
- backend/app/config.py, .env.example, pyproject.toml, frontend/package.json, pytest.ini updated as described
- backend/app/models/{__init__,user,company_profile}.py created; both tables in Base.metadata
- backend/alembic/versions/0001_create_users_company_profiles.py applied (alembic current == 0001)
- backend/app/services/bin_validator.py implements validate_bin with position-5 + two-cycle checksum
- backend/tests/test_bin_validation.py has 7+ tests all passing
- backend/tests/conftest.py exposes db_session fixture
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-company-profile/02-01-SUMMARY.md` summarising:
- New settings fields added
- Installed dependencies (versions)
- Models created and relationship loading strategy
- Migration revision ID
- BIN validator test count and chosen known-good test BIN
- Any deviations from RESEARCH.md
</output>
