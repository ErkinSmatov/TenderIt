---
phase: 04-document-vault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/services/minio_service.py
  - backend/app/models/document.py
  - backend/app/models/__init__.py
  - backend/app/schemas/document.py
  - backend/app/services/document_service.py
  - backend/app/main.py
  - backend/alembic/versions/0003_create_documents.py
  - backend/tests/test_documents.py
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
must_haves:
  truths:
    - "Приложение стартует и идемпотентно создаёт бакет tenderit-documents в MinIO"
    - "Таблица documents существует в БД после миграции 0003"
    - "compute_expiry_status возвращает корректный статус для всех граничных дат"
  artifacts:
    - path: "backend/app/services/minio_service.py"
      provides: "Minio singleton + ensure_bucket_exists + BUCKET_NAME"
      contains: "ensure_bucket_exists"
    - path: "backend/app/models/document.py"
      provides: "Document ORM model"
      contains: "class Document"
    - path: "backend/app/services/document_service.py"
      provides: "compute_expiry_status pure function"
      contains: "def compute_expiry_status"
    - path: "backend/alembic/versions/0003_create_documents.py"
      provides: "documents table migration"
      contains: "create_table"
    - path: "backend/tests/test_documents.py"
      provides: "Phase 4 test scaffold + expiry unit tests"
      contains: "compute_expiry_status"
  key_links:
    - from: "backend/app/main.py"
      to: "ensure_bucket_exists"
      via: "asyncio.to_thread in lifespan"
      pattern: "ensure_bucket_exists"
    - from: "backend/app/models/__init__.py"
      to: "Document"
      via: "import for Alembic visibility"
      pattern: "from app.models.document import Document"
---

<objective>
Wave 0 инфраструктура хранилища документов: добавить пакеты `minio` и
`python-multipart`, создать MinIO-сервис (singleton + ensure_bucket_exists),
ORM-модель Document, Pydantic-схемы, чистую функцию `compute_expiry_status`,
Alembic-миграцию 0003 и тест-скаффолд `test_documents.py`.

Purpose: без этой основы ни один backend-роут Phase 4 не запустится —
`python-multipart` обязателен для `UploadFile`, `minio` отсутствует в зависимостях,
а таблицы documents ещё нет.
Output: рабочий MinIO-сервис, модель, схемы, миграция, скаффолд тестов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-document-vault/04-CONTEXT.md
@.planning/phases/04-document-vault/04-RESEARCH.md
@.planning/phases/04-document-vault/04-PATTERNS.md
@CLAUDE.md

<interfaces>
Существующий Base и паттерн ORM — из backend/app/models/tender.py:
- `from app.db import Base`
- `Mapped[int] = mapped_column(primary_key=True)`
- TIMESTAMPTZ через `DateTime(timezone=True)`, server_default=func.now()

Существующий Settings (backend/app/config.py) уже содержит:
- `minio_endpoint: str = "localhost:9000"` (без схемы)
- `minio_access_key`, `minio_secret_key`
- ДОБАВИТЬ: `minio_secure: bool = False`

Текущий lifespan (backend/app/main.py lines 14-17):
- `async def lifespan(app): yield; await engine.dispose()`
- include_router список — добавить documents в Plan 02 (не здесь)

models/__init__.py текущий список: User, CompanyProfile, Tender, UserWatchlist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Зависимости + config + MinIO-сервис</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/services/minio_service.py</files>
  <read_first>
    - backend/pyproject.toml (текущий список dependencies)
    - backend/app/config.py (класс Settings)
    - backend/app/services/redis_service.py (паттерн singleton-сервиса)
    - .planning/phases/04-document-vault/04-RESEARCH.md (Pattern 1 + Критические находки #1, #2, #6)
  </read_first>
  <action>
    В `backend/pyproject.toml` добавить в dependencies: `minio>=7.2.14` и
    `python-multipart>=0.0.9`. В `backend/app/config.py` в класс Settings добавить
    поле `minio_secure: bool = False`.
    Создать `backend/app/services/minio_service.py`: импортировать `Minio` из `minio`
    и `settings` из `app.config`. Создать module-level singleton `_minio_client = Minio(...)`
    с `endpoint=settings.minio_endpoint`, `access_key=settings.minio_access_key`,
    `secret_key=settings.minio_secret_key`, `secure=settings.minio_secure`.
    Объявить `BUCKET_NAME = "tenderit-documents"`. Реализовать функцию
    `ensure_bucket_exists() -> None`: если `not _minio_client.bucket_exists(BUCKET_NAME)` —
    вызвать `_minio_client.make_bucket(BUCKET_NAME)`. Функция синхронная и идемпотентная.
    НЕ создавать Minio внутри функций (только singleton). НЕ хардкодить secure=False —
    брать из settings.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.minio_service import ensure_bucket_exists, BUCKET_NAME, _minio_client; assert BUCKET_NAME == 'tenderit-documents'" && grep -q "minio>=7.2.14" pyproject.toml && grep -q "python-multipart" pyproject.toml && grep -q "minio_secure" app/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` содержит `minio>=7.2.14` и `python-multipart>=0.0.9`
    - `backend/app/config.py` содержит поле `minio_secure: bool = False`
    - `minio_service.py` экспортирует `_minio_client`, `BUCKET_NAME`, `ensure_bucket_exists`
    - `python -c "from app.services.minio_service import ensure_bucket_exists, BUCKET_NAME"` exits 0
    - В коде ровно один вызов `Minio(` (singleton, не внутри функций)
  </acceptance_criteria>
  <done>minio + python-multipart в зависимостях, minio_secure в config, minio_service импортируется без ошибок.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document модель + схемы + document_service + миграция</name>
  <files>backend/app/models/document.py, backend/app/models/__init__.py, backend/app/schemas/document.py, backend/app/services/document_service.py, backend/alembic/versions/0003_create_documents.py</files>
  <read_first>
    - backend/app/models/tender.py (ORM-паттерн Mapped/mapped_column)
    - backend/app/models/__init__.py (список импортов и __all__)
    - backend/app/schemas/tender.py (Pydantic from_attributes паттерн)
    - backend/app/services/tender_service.py (паттерн pure utility _parse_gz_date)
    - backend/alembic/versions/0002_create_tenders_watchlist.py (паттерн миграции, revision/down_revision)
    - .planning/phases/04-document-vault/04-RESEARCH.md (Patterns 6, 7, 8 + Pitfall 6, 7)
    - .planning/phases/04-document-vault/04-PATTERNS.md (секции document.py / schemas / document_service / migration 0003)
  </read_first>
  <behavior>
    - compute_expiry_status(None) == "ok"
    - compute_expiry_status(now + 30 дней) == "ok"
    - compute_expiry_status(now + 10 дней) == "warning_14"
    - compute_expiry_status(now + 5 дней) == "warning_7"
    - compute_expiry_status(now - 1 день) == "expired"
  </behavior>
  <action>
    Создать `backend/app/models/document.py`: класс `Document(Base)`, `__tablename__ = "documents"`.
    Колонки: `id` (PK), `user_id` (ForeignKey "users.id" ondelete="CASCADE", nullable=False, index=True),
    `file_name` String(500), `file_key` String(1000), `file_size` Integer, `mime_type` String(200),
    `category` String(50), `expires_at` `DateTime(timezone=True)` nullable=True,
    `uploaded_at` `DateTime(timezone=True)` server_default=func.now() nullable=False. Без relationship.
    В `backend/app/models/__init__.py` добавить `from app.models.document import Document  # noqa: F401`
    и добавить "Document" в `__all__` (иначе Alembic не видит модель — Pitfall 6).
    Создать `backend/app/schemas/document.py`: `DocumentCategory(str, Enum)` со значениями
    ustav/license/certificate/registration/other; `ExpiryStatus = Literal["ok","warning_14","warning_7","expired"]`;
    `DocumentResponse(BaseModel)` с `model_config = {"from_attributes": True}` и полями
    id, file_name, file_key, file_size, mime_type, category (DocumentCategory), expires_at (datetime|None),
    uploaded_at (datetime), expiry_status (ExpiryStatus); `DocumentPatchRequest(BaseModel)` с
    category (DocumentCategory|None) и expires_at (datetime|None).
    Создать `backend/app/services/document_service.py`: импортировать ExpiryStatus или объявить локально,
    реализовать `compute_expiry_status(expires_at: datetime | None) -> ExpiryStatus`: если None → "ok";
    `now = datetime.now(timezone.utc)`; `days = (expires_at - now).days`; days<0 → "expired",
    days<=7 → "warning_7", days<=14 → "warning_14", иначе "ok". КРИТИЧНО: использовать
    `datetime.now(timezone.utc)` (tz-aware) — Pitfall 7.
    Создать миграцию `backend/alembic/versions/0003_create_documents.py` по паттерну 0002:
    `revision = "0003"`, `down_revision = "0002"`. `upgrade()` создаёт таблицу documents
    (колонки как в модели, ForeignKeyConstraint user_id→users.id ondelete CASCADE, PK id),
    индексы `ix_documents_user_id` на (user_id) и `ix_documents_user_expires` на (user_id, expires_at).
    `downgrade()` дропает индексы и таблицу.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models.document import Document; from app.schemas.document import DocumentCategory, DocumentResponse, DocumentPatchRequest; from app.services.document_service import compute_expiry_status; from app.models import Document as D2" && python -c "import app.models as m; assert 'Document' in m.__all__"</automated>
  </verify>
  <acceptance_criteria>
    - `Document` импортируется из `app.models.document` и из `app.models` (есть в __all__)
    - `DocumentCategory`, `DocumentResponse`, `DocumentPatchRequest` импортируются из `app.schemas.document`
    - `compute_expiry_status` использует `datetime.now(timezone.utc)` (источник содержит `timezone.utc`)
    - `0003_create_documents.py` содержит `revision = "0003"`, `down_revision = "0002"`, `create_table`, оба индекса
    - migration upgrade содержит `ondelete="CASCADE"` для user_id FK
  </acceptance_criteria>
  <done>Модель, схемы, сервис, миграция 0003 созданы; Document виден в app.models; compute_expiry_status tz-aware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lifespan hook + тест-скаффолд с unit-тестами expiry</name>
  <files>backend/app/main.py, backend/tests/test_documents.py</files>
  <read_first>
    - backend/app/main.py (текущий lifespan + include_router)
    - backend/tests/test_tenders.py (паттерн _register_and_login, authed/authed2 fixtures)
    - backend/tests/conftest.py (существующие fixtures client/auth_client/db_session)
    - .planning/phases/04-document-vault/04-RESEARCH.md (Pattern 2 + Validation Architecture / MinIO Mock Pattern)
  </read_first>
  <behavior>
    - test_expiry_status_logic покрывает все 5 веток compute_expiry_status (None, >14д, 8-14д, 1-7д, истёкший)
  </behavior>
  <action>
    В `backend/app/main.py` в `lifespan` ДО `yield` добавить
    `await asyncio.to_thread(ensure_bucket_exists)` (импорт `import asyncio` и
    `from app.services.minio_service import ensure_bucket_exists`). НЕ трогать
    include_router здесь — роут documents добавляется в Plan 02.
    Создать `backend/tests/test_documents.py` — тест-скаффолд Phase 4. Скопировать из
    `test_tenders.py` локальные фикстуры `authed` и `authed2` (через `_register_and_login`
    helper с уникальными prefix, например "doctest" / "doctest2"). Реализовать прямо сейчас
    pure-unit тест `test_expiry_status_logic()`: импортировать `compute_expiry_status` из
    `app.services.document_service`, проверить 5 случаев из behavior через
    `datetime.now(timezone.utc) + timedelta(...)`. Добавить заготовки (skip-маркеры или
    pass-заглушки с TODO-комментом) под будущие тесты Plan 02: test_upload_success,
    test_upload_too_large, test_upload_invalid_category, test_delete_document,
    test_delete_idor_protection, test_attachable_excludes_expired, test_get_presigned_url,
    test_url_idor_protection — реальные реализации создаст Plan 02.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_documents.py::test_expiry_status_logic -x -q && python -c "import asyncio; import app.main"</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` содержит `ensure_bucket_exists` и `asyncio.to_thread` в lifespan
    - `import app.main` exits 0 (нет ошибок импорта)
    - `pytest tests/test_documents.py::test_expiry_status_logic -x` exits 0
    - `test_documents.py` содержит фикстуры `authed` и `authed2`
  </acceptance_criteria>
  <done>lifespan вызывает ensure_bucket_exists; test_documents.py существует с зелёным expiry-тестом и authed-фикстурами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → MinIO | Backend инициализирует бакет при старте; учётки из settings |
| app → PostgreSQL | Миграция создаёт таблицу documents с FK на users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | minio_service бакет | mitigate | Бакет создаётся приватным (make_bucket без public policy); доступ только через pre-signed URL в Plan 02 |
| T-04-07 | Tampering | expiry_status вычисление | mitigate | `datetime.now(timezone.utc)` tz-aware; нельзя сравнить naive/aware (Pitfall 7), вычисление только в сервисе |
</threat_model>

<verification>
- `cd backend && pytest tests/test_documents.py -x` зелёный
- `cd backend && python -c "import app.main"` без ошибок
- Миграция 0003 имеет revision "0003", down_revision "0002"
</verification>

<success_criteria>
- minio + python-multipart добавлены в pyproject.toml
- minio_service.py с singleton + ensure_bucket_exists
- Document модель видна в app.models (Alembic-совместимость)
- compute_expiry_status проходит unit-тест для всех 5 веток
- lifespan вызывает ensure_bucket_exists через asyncio.to_thread
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-vault/04-01-SUMMARY.md`
</output>
