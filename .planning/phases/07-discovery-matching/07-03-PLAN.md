---
phase: 07-discovery-matching
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - backend/app/services/matching_service.py
  - backend/app/workers/tasks/run_matching.py
  - backend/app/workers/worker_settings.py
  - backend/app/routers/discovery.py
  - backend/app/main.py
  - backend/tests/test_matching_service.py
  - backend/tests/test_discovery_filters.py
  - backend/tests/test_discovery_matches.py
autonomous: true
requirements:
  - DISC-01
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "match_tenders_for_user() applies keyword ILIKE (OR-joined on name_ru/name_kz), region exact, spgz_codes IN, amount range filters"
    - "run_matching creates tender_match records with ON CONFLICT(user_id, tender_id) DO NOTHING"
    - "run_matching calls send_discovery_notification for each NEW match (if telegram_chat_id set, per D-03)"
    - "worker_settings.py registers poll_goszakup_discovery cron AND run_matching in functions"
    - "GET /api/discovery/matches returns only the current JWT user's matches (IDOR: WHERE user_id = current_user.id)"
    - "GET /api/discovery/filters returns 404 if no filter set yet"
    - "PUT /api/discovery/filters upserts (second PUT replaces first)"
    - "POST /api/discovery/{match_id}/participate returns 404 for matches belonging to other users"
    - "POST /api/discovery/{match_id}/skip returns 404 for matches belonging to other users"
    - "discovery router registered in main.py under /api prefix"
  artifacts:
    - path: backend/app/services/matching_service.py
      provides: "match_tenders_for_user() pure async function"
      exports: ["match_tenders_for_user"]
    - path: backend/app/workers/tasks/run_matching.py
      provides: "run_matching ARQ on-demand task"
      exports: ["run_matching"]
    - path: backend/app/workers/worker_settings.py
      provides: "Extended WorkerSettings with discovery cron + run_matching function"
    - path: backend/app/routers/discovery.py
      provides: "GET/PUT /api/discovery/filters, GET /api/discovery/matches, POST /api/discovery/{id}/participate, POST /api/discovery/{id}/skip"
      exports: ["router"]
  key_links:
    - from: backend/app/workers/tasks/run_matching.py
      to: backend/app/services/matching_service.py
      via: "from app.services.matching_service import match_tenders_for_user"
      pattern: "match_tenders_for_user"
    - from: backend/app/workers/tasks/run_matching.py
      to: backend/app/services/telegram_service.py
      via: "from app.services.telegram_service import send_discovery_notification"
      pattern: "send_discovery_notification"
    - from: backend/app/routers/discovery.py
      to: backend/app/services/application_service.py
      via: "from app.services.application_service import create_discovery_draft"
      pattern: "create_discovery_draft"
    - from: backend/app/main.py
      to: backend/app/routers/discovery.py
      via: "app.include_router(discovery.router, prefix='/api', tags=['discovery'])"
      pattern: "discovery.router"
---

<objective>
Build the matching engine and discovery CRUD API: matching_service.py pure function, run_matching ARQ task, worker_settings.py extension, and the discovery router covering filter management plus the match feed with participate/skip actions.

Purpose: This plan delivers the core "matching tenders to user filters" logic (DISC-03) plus the read/write API the frontend consumes (DISC-01, DISC-04). It runs in Wave 2 alongside 07-04 with no file overlap.

Output: Five new/extended Python files; three test files; discovery router registered in main.py.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-discovery-matching/07-CONTEXT.md
@.planning/phases/07-discovery-matching/07-RESEARCH.md
@.planning/phases/07-discovery-matching/07-01-SUMMARY.md
@.planning/phases/07-discovery-matching/07-02-SUMMARY.md

<interfaces>
From 07-01 (models and schemas created in Wave 1):
  from app.models.client_filter import ClientFilter
  from app.models.tender_match import TenderMatch
  from app.schemas.client_filter import ClientFilterCreate, ClientFilterResponse
  from app.schemas.tender_match import TenderMatchResponse
  ClientFilter columns: id, user_id, keywords (list[str]), spgz_codes (list[str]), region (str|None), min_amount, max_amount
  TenderMatch columns: id, user_id, tender_id, status, notified_at, decided_at, created_at, updated_at

From 07-02 (worker task created in Wave 1):
  from app.workers.tasks.poll_goszakup_discovery import poll_goszakup_discovery
  poll_goszakup_discovery is a cron task, enqueues "run_matching" with upserted_ids: list[int]

From backend/app/models/tender.py (extended in 07-01):
  Tender.name_ru, Tender.name_kz, Tender.total_sum, Tender.customer_name_ru,
  Tender.end_date (= submission deadline), Tender.region, Tender.spgz_code

From backend/app/workers/worker_settings.py (current state):
  cron_jobs = [cron(poll_watchlist_tenders, minute={0,5,...,55}, unique=True)]
  functions = [auto_submit_application]
  on_startup = startup, on_shutdown = shutdown

From backend/app/routers/applications.py (IDOR pattern to replicate for discovery):
  current_user_id = get_current_user(token) → int
  result = await db.execute(select(X).where(X.id == id, X.user_id == current_user_id))
  if not result: raise HTTPException(status_code=404)  # T-07-01/02: return 404 not 403

From backend/app/services/application_service.py (function added by 07-04, parallel plan):
  create_discovery_draft(db, user_id, tender_id) → Application
  NOTE: this function is created by plan 07-04 running in parallel. Import it in discovery.py.
  At runtime both 07-03 and 07-04 will have completed before the router is called.

From backend/app/services/telegram_service.py (function added by 07-04, parallel plan):
  send_discovery_notification(bot_token, chat_id, match_id, tender_name, customer_name, total_sum, deadline, region)
  NOTE: created by 07-04 running in parallel. Import in run_matching.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: matching_service.py + run_matching ARQ task + worker_settings extension</name>
  <files>
    backend/app/services/matching_service.py,
    backend/app/workers/tasks/run_matching.py,
    backend/app/workers/worker_settings.py,
    backend/tests/test_matching_service.py
  </files>
  <read_first>
    backend/app/workers/tasks/poll_watchlist.py (ctx['db_session_factory'] pattern),
    backend/app/workers/worker_settings.py (current state — add to existing lists),
    backend/tests/test_poll_watchlist.py (ARQ test pattern)
  </read_first>
  <behavior>
    - match_tenders_for_user returns tender when ANY keyword ILIKE matches name_ru
    - match_tenders_for_user returns tender when ANY keyword ILIKE matches name_kz
    - match_tenders_for_user excludes tender when region filter set and tender.region != cf.region
    - match_tenders_for_user excludes tender when spgz_codes filter set and tender.spgz_code not in cf.spgz_codes
    - match_tenders_for_user excludes tender below cf.min_amount
    - match_tenders_for_user excludes tender above cf.max_amount
    - match_tenders_for_user with all-NULL filter matches all provided tenders
    - Empty keywords list with non-null region: matches on region only
  </behavior>
  <action>
Create backend/app/services/matching_service.py:

async def match_tenders_for_user(
    db: AsyncSession,
    user_id: int,
    cf: ClientFilter,
    new_tender_ids: list[int],
) -> list[int]:
    """Return tender_ids from new_tender_ids that match cf rules.

    Rules applied in AND logic across filter types (all active filters must pass):
    - keywords: cf.keywords is non-empty → OR-join ILIKE on name_ru + name_kz; empty list = no keyword filter
    - region: cf.region is not None → exact match on tender.region
    - spgz_codes: cf.spgz_codes is non-empty → tender.spgz_code IN cf.spgz_codes
    - min_amount: cf.min_amount is not None → tender.total_sum >= cf.min_amount
    - max_amount: cf.max_amount is not None → tender.total_sum <= cf.max_amount
    """
    if not new_tender_ids:
        return []

    stmt = select(Tender).where(Tender.id.in_(new_tender_ids))

    # Keyword filter: OR-join ILIKE across name_ru and name_kz
    if cf.keywords:
        keyword_clauses = [
            or_(
                Tender.name_ru.ilike(f"%{kw}%"),
                Tender.name_kz.ilike(f"%{kw}%"),
            )
            for kw in cf.keywords
        ]
        stmt = stmt.where(or_(*keyword_clauses))

    # Region filter: exact match
    if cf.region is not None:
        stmt = stmt.where(Tender.region == cf.region)

    # СПГЗ code filter: tender.spgz_code must be in filter list
    if cf.spgz_codes:
        stmt = stmt.where(Tender.spgz_code.in_(cf.spgz_codes))

    # Amount range filters
    if cf.min_amount is not None:
        stmt = stmt.where(Tender.total_sum >= cf.min_amount)
    if cf.max_amount is not None:
        stmt = stmt.where(Tender.total_sum <= cf.max_amount)

    result = await db.execute(stmt)
    return [t.id for t in result.scalars().all()]

---

Create backend/app/workers/tasks/run_matching.py:

async def run_matching(ctx: dict, new_tender_ids: list[int]) -> None:
    """ARQ on-demand task: match new/updated tenders against all active user filters.

    Called by poll_goszakup_discovery after each successful upsert batch.
    Security: ON CONFLICT DO NOTHING prevents duplicate match records (Pitfall 3).
    """
    redis = ctx["redis"]
    async with ctx["db_session_factory"]() as session:
        # Load all client filters (one per user, D-10)
        result = await session.execute(select(ClientFilter))
        filters = result.scalars().all()

        if not filters:
            logger.debug("run_matching: no active client_filters, skipping")
            return

        for cf in filters:
            matching_ids = await match_tenders_for_user(session, cf.user_id, cf, new_tender_ids)

            for tender_id in matching_ids:
                # Insert with UNIQUE(user_id, tender_id) guard — Pitfall 3
                stmt = pg_insert(TenderMatch).values(
                    user_id=cf.user_id,
                    tender_id=tender_id,
                    status="matched",
                ).on_conflict_do_nothing(index_elements=["user_id", "tender_id"])
                result = await session.execute(stmt.returning(TenderMatch.id))
                new_match_id = result.scalar_one_or_none()
                await session.commit()

                if new_match_id is None:
                    # Already matched — skip notification
                    continue

                # Send Telegram notification if user has telegram_chat_id (D-03 guard)
                user = await session.get(User, cf.user_id)
                if user and user.telegram_chat_id:
                    tender = await session.get(Tender, tender_id)
                    try:
                        from app.config import settings
                        from app.services.telegram_service import send_discovery_notification
                        await send_discovery_notification(
                            bot_token=settings.telegram_bot_token,
                            chat_id=user.telegram_chat_id,
                            match_id=new_match_id,
                            tender_name=tender.name_ru,
                            customer_name=tender.customer_name_ru,
                            total_sum=tender.total_sum,
                            deadline=tender.end_date,  # Pitfall 4: end_date = deadline
                            region=tender.region,
                        )
                        # Update match status to notified
                        match = await session.get(TenderMatch, new_match_id)
                        if match:
                            match.status = "notified"
                            match.notified_at = utcnow()
                            await session.commit()
                    except Exception as exc:
                        # Telegram failure must NOT block matching (same pattern as poll_watchlist)
                        logger.warning("run_matching: telegram send failed for match %s: %s", new_match_id, exc)

Imports needed in run_matching.py:
  from sqlalchemy import select
  from sqlalchemy.dialects.postgresql import insert as pg_insert
  from app.models.client_filter import ClientFilter
  from app.models.tender_match import TenderMatch
  from app.models.tender import Tender
  from app.models.user import User
  from app.services.matching_service import match_tenders_for_user
  (send_discovery_notification imported inline to allow parallel execution with 07-04)

---

Extend backend/app/workers/worker_settings.py:
Add two new imports at the top:
  from app.workers.tasks.poll_goszakup_discovery import poll_goszakup_discovery
  from app.workers.tasks.run_matching import run_matching

Change cron_jobs list to add the discovery poll:
  cron_jobs = [
      cron(poll_watchlist_tenders, minute={0,5,10,15,20,25,30,35,40,45,50,55}, unique=True),
      cron(poll_goszakup_discovery, minute={0, 15, 30, 45}, unique=True),  # D-06
  ]

Change functions list to add run_matching:
  functions = [auto_submit_application, run_matching]

Update the module docstring to mention the new jobs.

---

Create backend/tests/test_matching_service.py:
Unit tests using pytest-asyncio. Use in-memory Tender objects (mock or test DB session).
Write tests BEFORE implementing (TDD): each test should fail with NotImplementedError or AttributeError before matching_service.py is complete.

Tests (all unit-level, use test DB session or mocked async session):
1. keyword_match_name_ru: tender with name_ru containing "строительство", filter has keywords=["строительство"] → returns tender.id
2. keyword_match_name_kz: tender with name_kz containing keyword → returns tender.id
3. keyword_no_match: tender name_ru doesn't contain keyword → returns []
4. keyword_or_logic: two keywords, tender matches second → returns tender.id
5. region_match: cf.region="Алматы", tender.region="Алматы" → returns tender.id
6. region_mismatch: cf.region="Астана", tender.region="Алматы" → returns []
7. amount_range_match: cf.min_amount=100, cf.max_amount=500, tender.total_sum=300 → returns tender.id
8. amount_below_min: tender.total_sum=50, cf.min_amount=100 → returns []
9. all_null_filter: cf.keywords=[], region=None, spgz_codes=[], min=None, max=None → returns all tender IDs
10. spgz_filter: cf.spgz_codes=["12.34.56"], tender.spgz_code="12.34.56" → returns tender.id
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -c "from app.services.matching_service import match_tenders_for_user; from app.workers.tasks.run_matching import run_matching; print('OK')" && pytest tests/test_matching_service.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    matching_service.py and run_matching.py import cleanly. All 10 test_matching_service.py tests pass. worker_settings.py has two cron_jobs and two entries in functions list. ON CONFLICT DO NOTHING pattern confirmed in run_matching.py. utcnow() helper imported or defined locally (following application_service.py pattern).
  </done>
</task>

<task type="auto">
  <name>Task 2: discovery.py router + main.py registration + filter/match integration tests</name>
  <files>
    backend/app/routers/discovery.py,
    backend/app/main.py,
    backend/tests/test_discovery_filters.py,
    backend/tests/test_discovery_matches.py
  </files>
  <read_first>
    backend/app/routers/applications.py (IDOR check pattern, current_user usage, get_db dependency),
    backend/app/routers/tenders.py (router structure pattern),
    backend/app/main.py (existing include_router calls to replicate — add after telegram_webhook line)
  </read_first>
  <action>
Create backend/app/routers/discovery.py with five endpoints, all auth-gated via get_current_user:

Imports needed:
  from app.db import get_db
  from app.models.client_filter import ClientFilter
  from app.models.tender_match import TenderMatch
  from app.models.tender import Tender
  from app.schemas.client_filter import ClientFilterCreate, ClientFilterResponse
  from app.schemas.tender_match import TenderMatchResponse
  from app.services.application_service import create_discovery_draft
  from app.schemas.application import ApplicationResponse

router = APIRouter()

1. GET /api/discovery/filters:
   Load ClientFilter WHERE user_id == current_user.id.
   Return ClientFilterResponse. If not found: raise HTTPException(404, detail="Фильтры не настроены").

2. PUT /api/discovery/filters:
   Body: ClientFilterCreate. Upsert semantics:
   stmt = pg_insert(ClientFilter).values(user_id=user_id, **data.model_dump())
           .on_conflict_do_update(index_elements=['user_id'], set_={column: data.field ...})
   await db.execute(stmt); await db.commit()
   Reload and return ClientFilterResponse.

3. GET /api/discovery/matches:
   Join TenderMatch with Tender to populate denormalized fields.
   SELECT tender_match.*, tender.name_ru, tender.customer_name_ru, tender.total_sum,
          tender.end_date, tender.region
   WHERE tender_match.user_id == current_user.id
   ORDER BY tender_match.created_at DESC
   Build TenderMatchResponse list with tender fields mapped. Return list[TenderMatchResponse].
   IDOR: filter is enforced by WHERE user_id == current_user.id (T-07-03).

4. POST /api/discovery/{match_id}/participate:
   Load TenderMatch WHERE id == match_id AND user_id == current_user.id.
   If not found: raise HTTPException(404)  # T-07-01: return 404 not 403
   If match.status in ('participating', 'skipped'): return HTTPException(409, detail="Уже обработано")
   Call create_discovery_draft(db, current_user.id, match.tender_id) → Application
   Update match.status = 'participating', match.decided_at = utcnow(). commit.
   Return ApplicationResponse.model_validate(app) with status_code=201.

5. POST /api/discovery/{match_id}/skip:
   Load TenderMatch WHERE id == match_id AND user_id == current_user.id.
   If not found: raise HTTPException(404)  # T-07-02: return 404 not 403
   If match.status == 'skipped': return HTTPException(409, detail="Уже пропущено")
   Update match.status = 'skipped', match.decided_at = utcnow(). commit.
   Return {"ok": True}.

IMPORTANT: Use utcnow() helper — define it inline or import from application_service:
  def _utcnow(): return datetime.now(timezone.utc).replace(tzinfo=None)

---

Extend backend/app/main.py:
Add import: from app.routers import discovery
Add after the telegram_webhook include_router line:
  application.include_router(discovery.router, prefix="/api", tags=["discovery"])

---

Create backend/tests/test_discovery_filters.py:
Integration tests using the test client and auth_client fixture from conftest.py.
Pattern: register test user → login → PUT /api/discovery/filters → GET → verify.
Tests:
1. PUT creates new filter: keywords=["тест"], returns 200 with ClientFilterResponse
2. GET returns saved filter: keywords=["тест"]
3. Second PUT replaces first: keywords=["новый"] overwrites keywords=["тест"]
4. GET on fresh user with no filter returns 404
5. Unauthenticated GET returns 401

Create backend/tests/test_discovery_matches.py:
IDOR integration tests.
Tests:
1. User A cannot see User B's match: create match for user B, call GET /api/discovery/matches as user A → returns empty list (not 404, not user B's match)
2. POST /api/discovery/{match_id}/participate with match owned by user B → 404 (T-07-01)
3. POST /api/discovery/{match_id}/skip with match owned by user B → 404 (T-07-02)
4. Valid participate: creates Application with status='draft', returns ApplicationResponse, match status becomes 'participating'
5. Valid skip: returns {"ok": True}, match status becomes 'skipped'
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -c "from app.routers.discovery import router; print('router OK')" && pytest tests/test_discovery_filters.py tests/test_discovery_matches.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    discovery router imports cleanly and is registered in main.py. All test_discovery_filters.py and test_discovery_matches.py tests pass. IDOR tests confirm user A cannot access user B's matches (returns 404 or empty list, not the data). Second PUT replaces first filter. create_discovery_draft is called from POST /participate and returns ApplicationResponse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT → user_id | JWT claim provides user_id; all queries filter by this value |
| HTTP client → /api/discovery/* | Unauthenticated or cross-user access attempts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | POST /api/discovery/{match_id}/participate | mitigate | Load TenderMatch WHERE id=match_id AND user_id=current_user.id; return 404 (not 403) on mismatch — does not leak resource existence |
| T-07-02 | Elevation of Privilege | POST /api/discovery/{match_id}/skip | mitigate | Same pattern as T-07-01: WHERE user_id filter + 404 on mismatch |
| T-07-03 | Information Disclosure | GET /api/discovery/matches | mitigate | SELECT ... WHERE tender_match.user_id = current_user.id enforced in SQLAlchemy; executor must not pass user_id from request body |
| T-07-ilike | Tampering | ILIKE keyword filter in matching_service | accept | SQLAlchemy .ilike() uses parameterized queries — PostgreSQL % in ILIKE is a valid wildcard, not injectable; low risk |
</threat_model>

<verification>
1. `python -c "from app.routers.discovery import router; from app.main import app; print('OK')"` — no ImportError
2. `pytest tests/test_matching_service.py tests/test_discovery_filters.py tests/test_discovery_matches.py -x` — all pass
3. `grep "run_matching" backend/app/workers/worker_settings.py` — appears in both functions list and imports
4. `grep "poll_goszakup_discovery" backend/app/workers/worker_settings.py` — appears in cron_jobs
5. `grep -n "user_id.*current_user" backend/app/routers/discovery.py` — confirms IDOR WHERE clauses in all three read/action endpoints
</verification>

<success_criteria>
- match_tenders_for_user implements ILIKE OR-joined keywords, region exact, spgz_codes IN, amount range — all 10 unit tests pass
- run_matching uses ON CONFLICT(user_id, tender_id) DO NOTHING when inserting matches
- run_matching calls send_discovery_notification only when user.telegram_chat_id is not None (D-03)
- worker_settings.py has two cron entries and two function entries
- GET /api/discovery/matches returns empty list for user with no matches (not 404)
- POST participate and skip return 404 for cross-user access (T-07-01, T-07-02)
- PUT /filters upsert tested: second PUT replaces first
- discovery.router registered in main.py
</success_criteria>

<output>
After completion, create .planning/phases/07-discovery-matching/07-03-SUMMARY.md
</output>
