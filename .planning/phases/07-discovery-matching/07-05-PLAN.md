---
phase: 07-discovery-matching
plan: 05
type: execute
wave: 3
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - frontend/src/types/discovery.ts
  - frontend/src/components/discovery/TenderMatchStatusBadge.tsx
  - frontend/src/components/discovery/TenderMatchCard.tsx
  - frontend/src/app/(dashboard)/discovery/page.tsx
  - frontend/src/app/(dashboard)/discovery-filters/page.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/middleware.ts
  - frontend/.env.example
autonomous: true
requirements:
  - DISC-04
  - DISC-06

must_haves:
  truths:
    - "Unauthenticated users are redirected to /login when accessing /discovery or /discovery-filters"
    - "Authenticated users see a discovery feed page at /discovery with TenderMatchCard list"
    - "Empty discovery feed shows 'Настройте фильтры' message with link to /discovery-filters"
    - "Filter settings page at /discovery-filters has keyword, СПГЗ codes, region, min/max amount fields"
    - "Submitting /discovery-filters form calls PUT /api/discovery/filters and shows 'Фильтры сохранены'"
    - "TenderMatchStatusBadge maps: matched→Новый(blue), notified→Уведомлён(amber), participating→Участвуем(green), skipped→Пропущен(gray)"
    - "TenderMatchCard 'Участвуем' button is hidden when status is 'participating' or 'skipped'"
    - "TenderMatchCard 'Пропустить' button is hidden when status is 'skipped' or 'participating'"
    - "Sidebar shows 'Подборка' nav link at /discovery (with SparklesIcon)"
    - "Sidebar shows 'Telegram бот' external link using NEXT_PUBLIC_TELEGRAM_BOT_USERNAME"
    - "/discovery and /discovery-filters are in middleware.ts protectedRoutes"
  artifacts:
    - path: frontend/src/types/discovery.ts
      provides: "TenderMatchStatus, TenderMatchResponse, ClientFilterResponse TypeScript types"
    - path: frontend/src/components/discovery/TenderMatchStatusBadge.tsx
      provides: "Status badge component with 4 states"
    - path: frontend/src/components/discovery/TenderMatchCard.tsx
      provides: "Match card with action buttons"
    - path: frontend/src/app/(dashboard)/discovery/page.tsx
      provides: "Discovery feed page with useQuery"
    - path: frontend/src/app/(dashboard)/discovery-filters/page.tsx
      provides: "Filter settings form with useMutation"
    - path: frontend/src/components/layout/Sidebar.tsx
      provides: "Extended sidebar with Подборка nav + Telegram bot link"
    - path: frontend/src/middleware.ts
      provides: "protectedRoutes includes /discovery and /discovery-filters"
  key_links:
    - from: frontend/src/app/(dashboard)/discovery/page.tsx
      to: "/api/discovery/matches"
      via: "useQuery → api.get('/api/discovery/matches')"
      pattern: "api\\.get.*discovery/matches"
    - from: frontend/src/app/(dashboard)/discovery-filters/page.tsx
      to: "/api/discovery/filters"
      via: "useMutation → api.put('/api/discovery/filters', data)"
      pattern: "api\\.put.*discovery/filters"
    - from: frontend/src/components/discovery/TenderMatchCard.tsx
      to: "/api/discovery/{id}/participate"
      via: "POST on button click"
      pattern: "discovery.*participate"
---

<objective>
Build the complete frontend for Phase 7: TypeScript types, two discovery components, two new pages (feed + filter settings), sidebar extension, and middleware update.

Purpose: Delivers the user-facing discovery experience (DISC-04: feed, DISC-06: Telegram bot link) that consumes the backend endpoints built in 07-03 and 07-04.

Output: 7 new/modified files; no new npm packages required.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-discovery-matching/07-CONTEXT.md
@.planning/phases/07-discovery-matching/07-RESEARCH.md
@.planning/phases/07-discovery-matching/07-03-SUMMARY.md
@.planning/phases/07-discovery-matching/07-04-SUMMARY.md

<interfaces>
From frontend/src/components/applications/ApplicationStatusBadge.tsx (pattern to replicate exactly):
  STATUS_CONFIG: Record<Status, { label: string; className: string }>
  <span className={cn('inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium', ...)}

From frontend/src/app/(dashboard)/applications/page.tsx (page pattern to mirror):
  'use client'
  useQuery<T[]>({ queryKey: [...], queryFn: () => api.get<T[]>('/api/...'), retry: false })
  Error: <Alert className="text-destructive border-destructive/50 bg-destructive/10 text-sm">
  Loading: <p className="text-sm text-muted-foreground">Загрузка...</p>
  Empty state: <div className="rounded-lg border border-dashed border-border py-12 text-center">

From frontend/src/components/layout/Sidebar.tsx (current navItems array):
  { href: '/dashboard', label: 'Обзор', icon: LayoutDashboard },
  { href: '/tenders', label: 'Тендеры', icon: Search },
  { href: '/applications', label: 'Заявки', icon: ClipboardList },
  { href: '/profile', label: 'Профиль', icon: Building2 },
  { href: '/documents', label: 'Документы', icon: FileText },
  Logout section is at the bottom in a border-t div.
  Import from lucide-react: currently LayoutDashboard, Search, Building2, FileText, ClipboardList, LogOut

From frontend/src/middleware.ts (current state):
  const protectedRoutes = ['/dashboard', '/profile', '/tenders', '/applications', '/documents']
  (Research pitfall 6: /discovery is NOT in this list — must add)

From frontend/src/lib/api.ts (api client pattern):
  api.get<T>(path), api.post<T>(path, body), api.put<T>(path, body) — all return Promise<T>
  (verify actual method names from existing usage in applications/page.tsx)

Backend API endpoints now available (from 07-03):
  GET /api/discovery/matches → list of TenderMatchResponse (with denormalized tender fields)
  GET /api/discovery/filters → ClientFilterResponse (404 if not set)
  PUT /api/discovery/filters → ClientFilterResponse
  POST /api/discovery/{match_id}/participate → ApplicationResponse (201)
  POST /api/discovery/{match_id}/skip → {"ok": true}

TenderMatchResponse fields (from 07-01 schemas + 07-03 JOIN):
  id, user_id, tender_id, status: TenderMatchStatus, notified_at, decided_at, created_at,
  tender_name_ru, customer_name_ru, total_sum (Decimal string), end_date (ISO datetime), region

Locked decisions:
  D-08: Sidebar Telegram bot link uses NEXT_PUBLIC_TELEGRAM_BOT_USERNAME env var, opens in new tab
  D-12: /discovery shows feed, newest-first; status badge + action buttons per card
  Research pitfall 6: add '/discovery' AND '/discovery-filters' to protectedRoutes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types + components + Sidebar + middleware + .env.example</name>
  <files>
    frontend/src/types/discovery.ts,
    frontend/src/components/discovery/TenderMatchStatusBadge.tsx,
    frontend/src/components/discovery/TenderMatchCard.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/middleware.ts,
    frontend/.env.example
  </files>
  <read_first>
    frontend/src/types/application.ts (pattern for TypeScript types),
    frontend/src/components/applications/ApplicationStatusBadge.tsx (STATUS_CONFIG pattern — replicate exactly),
    frontend/src/components/applications/ApplicationCard.tsx (card layout pattern),
    frontend/src/components/layout/Sidebar.tsx (read fully before modifying),
    frontend/src/middleware.ts (read fully before modifying — change protectedRoutes array only)
  </read_first>
  <action>
Create frontend/src/types/discovery.ts:

export type TenderMatchStatus = 'matched' | 'notified' | 'skipped' | 'participating'

export interface TenderMatchResponse {
  id: number
  user_id: number
  tender_id: number
  status: TenderMatchStatus
  notified_at: string | null   // ISO datetime
  decided_at: string | null    // ISO datetime
  created_at: string           // ISO datetime
  // Denormalized tender fields (from JOIN in discovery router)
  tender_name_ru: string | null
  customer_name_ru: string | null
  total_sum: string | null     // Decimal as string (e.g. "1500000.00")
  end_date: string | null      // ISO datetime (= submission deadline, Research pitfall 4)
  region: string | null
}

export interface ClientFilterResponse {
  id: number
  user_id: number
  keywords: string[]
  spgz_codes: string[]
  region: string | null
  min_amount: string | null    // Decimal as string
  max_amount: string | null    // Decimal as string
  created_at: string
  updated_at: string
}

---

Create frontend/src/components/discovery/TenderMatchStatusBadge.tsx:
Mirrors ApplicationStatusBadge.tsx exactly.
STATUS_CONFIG:
  matched:      { label: 'Новый',      className: 'bg-blue-100 text-blue-700 border-blue-200' }
  notified:     { label: 'Уведомлён',  className: 'bg-amber-100 text-amber-700 border-amber-200' }
  participating:{ label: 'Участвуем',  className: 'bg-green-100 text-green-700 border-green-200' }
  skipped:      { label: 'Пропущен',   className: 'bg-gray-100 text-gray-600 border-gray-200' }

interface TenderMatchStatusBadgeProps { status: TenderMatchStatus; className?: string }
export default function TenderMatchStatusBadge({ status, className }: TenderMatchStatusBadgeProps) {
  const { label, className: statusClassName } = STATUS_CONFIG[status]
  return <span className={cn('inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium', statusClassName, className)}>{label}</span>
}

---

Create frontend/src/components/discovery/TenderMatchCard.tsx:
'use client'
Props: { match: TenderMatchResponse }

Display fields:
- Title: match.tender_name_ru || 'Тендер без названия'
- Customer: match.customer_name_ru || 'Заказчик не указан'
- Amount: format match.total_sum as currency (e.g. parseFloat(match.total_sum).toLocaleString('ru-RU') + ' ₸') or 'Сумма не указана' if null
- Deadline: format match.end_date as date string (e.g. new Date(match.end_date).toLocaleDateString('ru-RU')) or 'Дедлайн не указан' if null
- Region: match.region || 'Регион не указан'
- Source badge: static 'goszakup' gray badge
- Status badge: <TenderMatchStatusBadge status={match.status} />

Action buttons (use useMutation from @tanstack/react-query):
- "Участвуем" button: calls POST /api/discovery/{match.id}/participate
  On success: redirects to /applications/{response.id} (use router.push)
  Hide when status === 'participating' || status === 'skipped'
  Show loading state while pending (disabled + "Подождите...")
- "Пропустить" button: calls POST /api/discovery/{match.id}/skip
  On success: invalidate 'discovery-matches' query (queryClient.invalidateQueries)
  Hide when status === 'skipped' || status === 'participating'
  Show loading state while pending

Use buttonVariants from @/components/ui/button for button styling.
Use useRouter from next/navigation, useQueryClient from @tanstack/react-query.
Import api from @/lib/api.

Card layout: follow ApplicationCard.tsx pattern (rounded-lg border border-border p-4).

---

Modify frontend/src/components/layout/Sidebar.tsx:
1. Add SparklesIcon and ExternalLink to lucide-react imports
2. Add to navItems array AFTER { href: '/applications', ... }:
   { href: '/discovery', label: 'Подборка', icon: SparklesIcon }
3. In the bottom section (border-t div that has the Logout button), add BEFORE the logout button:
   <a
     href={`https://t.me/${process.env.NEXT_PUBLIC_TELEGRAM_BOT_USERNAME}`}
     target="_blank"
     rel="noopener noreferrer"
     className="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium text-muted-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground transition-colors"
   >
     <ExternalLink className="h-4 w-4 shrink-0" aria-hidden="true" />
     Telegram бот
   </a>
4. Do NOT change any existing navItems, className patterns, or the logout handler.

---

Modify frontend/src/middleware.ts:
Change protectedRoutes to add '/discovery' and '/discovery-filters':
  const protectedRoutes = ['/dashboard', '/profile', '/tenders', '/applications', '/documents', '/discovery', '/discovery-filters']
No other changes.

---

Modify frontend/.env.example:
Add line: NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=your_bot_username
(Place near other NEXT_PUBLIC_ variables or at the end of the file.)
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/frontend && npx tsc --noEmit 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>
    TypeScript compiles without errors on the new files. TenderMatchStatusBadge has all 4 statuses. Sidebar.tsx has SparklesIcon + /discovery nav item + Telegram bot external link. middleware.ts has /discovery and /discovery-filters in protectedRoutes array. .env.example has NEXT_PUBLIC_TELEGRAM_BOT_USERNAME entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Discovery feed page + Filter settings page</name>
  <files>
    frontend/src/app/(dashboard)/discovery/page.tsx,
    frontend/src/app/(dashboard)/discovery-filters/page.tsx
  </files>
  <read_first>
    frontend/src/app/(dashboard)/applications/page.tsx (useQuery pattern, empty state, error Alert pattern),
    frontend/src/components/discovery/TenderMatchCard.tsx (just created in Task 1),
    frontend/src/types/discovery.ts (just created in Task 1)
  </read_first>
  <action>
Create frontend/src/app/(dashboard)/discovery/page.tsx:
'use client'

import Link from 'next/link'
import { useQuery } from '@tanstack/react-query'
import { api } from '@/lib/api'
import type { TenderMatchResponse } from '@/types/discovery'
import TenderMatchCard from '@/components/discovery/TenderMatchCard'
import { Alert } from '@/components/ui/alert'

export default function DiscoveryPage() {
  const { data, error, isLoading } = useQuery<TenderMatchResponse[]>({
    queryKey: ['discovery-matches'],
    queryFn: () => api.get<TenderMatchResponse[]>('/api/discovery/matches'),
    retry: false,
  })

  // Sort newest-first by created_at (mirrors applications/page.tsx pattern)
  const sorted = data
    ? [...data].sort((a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime())
    : []

  return (
    <div className="space-y-6 max-w-2xl">
      <div className="flex items-center justify-between gap-4">
        <div>
          <h1 className="text-xl font-semibold">Подборка</h1>
          <p className="text-sm text-muted-foreground mt-0.5">
            Тендеры по вашим фильтрам
          </p>
        </div>
        <Link href="/discovery-filters" className="text-sm text-primary hover:underline">
          Настроить фильтры
        </Link>
      </div>

      {error && (
        <Alert className="text-destructive border-destructive/50 bg-destructive/10 text-sm">
          Не удалось загрузить подборку. Проверьте соединение и попробуйте ещё раз.
        </Alert>
      )}

      {isLoading && (
        <p className="text-sm text-muted-foreground">Загрузка...</p>
      )}

      {!isLoading && !error && sorted.length === 0 && (
        <div className="rounded-lg border border-dashed border-border py-12 text-center">
          <p className="text-sm text-muted-foreground">Подходящих тендеров пока нет</p>
          <p className="text-xs text-muted-foreground mt-1">
            <Link href="/discovery-filters" className="underline">Настройте фильтры</Link>,
            чтобы получать подборку тендеров
          </p>
        </div>
      )}

      {!isLoading && !error && sorted.length > 0 && (
        <div className="space-y-4">
          {sorted.map((match) => (
            <TenderMatchCard key={match.id} match={match} />
          ))}
        </div>
      )}
    </div>
  )
}

---

Create frontend/src/app/(dashboard)/discovery-filters/page.tsx:
'use client'

import { useEffect, useState } from 'react'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { api } from '@/lib/api'
import type { ClientFilterResponse } from '@/types/discovery'
import { Alert } from '@/components/ui/alert'

interface FilterFormData {
  keywordsRaw: string       // comma-separated string input
  spgzCodesRaw: string      // comma-separated string input
  region: string
  minAmount: string         // numeric as string (empty = no limit)
  maxAmount: string
}

export default function DiscoveryFiltersPage() {
  const queryClient = useQueryClient()

  const { data: currentFilter, isLoading } = useQuery<ClientFilterResponse>({
    queryKey: ['discovery-filters'],
    queryFn: () => api.get<ClientFilterResponse>('/api/discovery/filters'),
    retry: false,  // 404 = no filter yet, not an error to retry
  })

  const [form, setForm] = useState<FilterFormData>({
    keywordsRaw: '',
    spgzCodesRaw: '',
    region: '',
    minAmount: '',
    maxAmount: '',
  })
  const [saved, setSaved] = useState(false)

  // Pre-fill form when existing filter loads
  useEffect(() => {
    if (currentFilter) {
      setForm({
        keywordsRaw: currentFilter.keywords.join(', '),
        spgzCodesRaw: currentFilter.spgz_codes.join(', '),
        region: currentFilter.region || '',
        minAmount: currentFilter.min_amount || '',
        maxAmount: currentFilter.max_amount || '',
      })
    }
  }, [currentFilter])

  const mutation = useMutation({
    mutationFn: (data: object) => api.put<ClientFilterResponse>('/api/discovery/filters', data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['discovery-filters'] })
      setSaved(true)
      setTimeout(() => setSaved(false), 3000)
    },
  })

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    const keywords = form.keywordsRaw.split(',').map(k => k.trim()).filter(Boolean)
    const spgz_codes = form.spgzCodesRaw.split(',').map(s => s.trim()).filter(Boolean)
    mutation.mutate({
      keywords,
      spgz_codes,
      region: form.region.trim() || null,
      min_amount: form.minAmount ? parseFloat(form.minAmount) : null,
      max_amount: form.maxAmount ? parseFloat(form.maxAmount) : null,
    })
  }

  return (
    <div className="max-w-lg space-y-6">
      <div>
        <h1 className="text-xl font-semibold">Фильтры подборки</h1>
        <p className="text-sm text-muted-foreground mt-0.5">
          Тендеры, совпадающие с фильтрами, появятся в «Подборке» и придут в Telegram
        </p>
      </div>

      {isLoading && <p className="text-sm text-muted-foreground">Загрузка...</p>}

      {!isLoading && (
        <form onSubmit={handleSubmit} className="space-y-4">
          <div className="space-y-1">
            <label className="text-sm font-medium">Ключевые слова</label>
            <input
              type="text"
              value={form.keywordsRaw}
              onChange={e => setForm(f => ({ ...f, keywordsRaw: e.target.value }))}
              placeholder="строительство, ремонт, услуги"
              className="w-full rounded-md border border-input bg-background px-3 py-2 text-sm"
            />
            <p className="text-xs text-muted-foreground">Введите через запятую. Тендер подойдёт, если название содержит любое слово.</p>
          </div>

          <div className="space-y-1">
            <label className="text-sm font-medium">СПГЗ коды</label>
            <input
              type="text"
              value={form.spgzCodesRaw}
              onChange={e => setForm(f => ({ ...f, spgzCodesRaw: e.target.value }))}
              placeholder="12.34.56, 78.90.12"
              className="w-full rounded-md border border-input bg-background px-3 py-2 text-sm"
            />
            <p className="text-xs text-muted-foreground">Точное совпадение кода СПГЗ/КТРУ.</p>
          </div>

          <div className="space-y-1">
            <label className="text-sm font-medium">Регион</label>
            <input
              type="text"
              value={form.region}
              onChange={e => setForm(f => ({ ...f, region: e.target.value }))}
              placeholder="Алматы"
              className="w-full rounded-md border border-input bg-background px-3 py-2 text-sm"
            />
          </div>

          <div className="grid grid-cols-2 gap-3">
            <div className="space-y-1">
              <label className="text-sm font-medium">Сумма от (₸)</label>
              <input
                type="number"
                min="0"
                value={form.minAmount}
                onChange={e => setForm(f => ({ ...f, minAmount: e.target.value }))}
                placeholder="0"
                className="w-full rounded-md border border-input bg-background px-3 py-2 text-sm"
              />
            </div>
            <div className="space-y-1">
              <label className="text-sm font-medium">Сумма до (₸)</label>
              <input
                type="number"
                min="0"
                value={form.maxAmount}
                onChange={e => setForm(f => ({ ...f, maxAmount: e.target.value }))}
                placeholder="Без ограничений"
                className="w-full rounded-md border border-input bg-background px-3 py-2 text-sm"
              />
            </div>
          </div>

          {mutation.isError && (
            <Alert className="text-destructive border-destructive/50 bg-destructive/10 text-sm">
              Не удалось сохранить фильтры. Попробуйте снова.
            </Alert>
          )}

          {saved && (
            <p className="text-sm text-green-600">Фильтры сохранены</p>
          )}

          <button
            type="submit"
            disabled={mutation.isPending}
            className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90 disabled:opacity-50"
          >
            {mutation.isPending ? 'Сохраняем...' : 'Сохранить'}
          </button>
        </form>
      )}
    </div>
  )
}
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS|Error" | head -10 || echo "TypeScript OK"</automated>
  </verify>
  <done>
    Both pages compile without TypeScript errors. /discovery renders TenderMatchCard list with useQuery. /discovery-filters renders the filter form that pre-fills from GET /api/discovery/filters. Empty state on /discovery shows link to /discovery-filters. "Фильтры сохранены" appears after successful PUT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js middleware | Route protection for /discovery and /discovery-filters |
| Browser → /api/discovery/* | Authenticated requests via HTTP-only cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-mw-01 | Elevation of Privilege | /discovery, /discovery-filters middleware | mitigate | Both routes added to protectedRoutes in middleware.ts — unauthenticated requests redirected to /login (Research pitfall 6 fix) |
| T-07-mw-02 | Information Disclosure | TenderMatchCard "Участвуем" POST | accept | POST /api/discovery/{id}/participate is IDOR-protected server-side (07-03); frontend cannot access another user's match_id without knowing it — 404 is returned |
| T-07-ext-04 | Spoofing | NEXT_PUBLIC_TELEGRAM_BOT_USERNAME | accept | This env var controls only the display link, not any auth flow. User clicks a t.me link — Telegram handles identity. No security risk in client-side exposure. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — no TypeScript errors
2. Visit /discovery without auth cookie → redirected to /login (middleware check)
3. Visit /discovery-filters without auth cookie → redirected to /login (middleware check)
4. Authenticated: visit /discovery → page renders (empty state if no matches)
5. Authenticated: visit /discovery-filters → form renders, save works, shows "Фильтры сохранены"
6. Sidebar: "Подборка" link visible with SparklesIcon, "Telegram бот" link in bottom section
7. `grep "/discovery" frontend/src/middleware.ts` — appears in protectedRoutes
8. `grep "NEXT_PUBLIC_TELEGRAM_BOT_USERNAME" frontend/.env.example` — appears
</verification>

<success_criteria>
- TypeScript compiles without errors on all 7 new/modified files
- /discovery and /discovery-filters are protected routes (added to middleware.ts)
- TenderMatchStatusBadge has all 4 statuses with correct Russian labels and Tailwind colors
- TenderMatchCard hides action buttons when status is 'participating' or 'skipped'
- /discovery empty state shows link to /discovery-filters
- /discovery-filters pre-fills saved filter and shows "Фильтры сохранены" on success
- Sidebar has "Подборка" nav item AND "Telegram бот" external link with NEXT_PUBLIC_TELEGRAM_BOT_USERNAME
- NEXT_PUBLIC_TELEGRAM_BOT_USERNAME added to frontend/.env.example
</success_criteria>

<output>
After completion, create .planning/phases/07-discovery-matching/07-05-SUMMARY.md
</output>
