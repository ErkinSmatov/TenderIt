---
phase: 07-discovery-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0005_extend_tenders_source_fields.py
  - backend/alembic/versions/0006_create_client_filters.py
  - backend/alembic/versions/0007_create_tender_matches.py
  - backend/app/models/tender.py
  - backend/app/models/client_filter.py
  - backend/app/models/tender_match.py
  - backend/app/schemas/client_filter.py
  - backend/app/schemas/tender_match.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03

must_haves:
  truths:
    - "alembic upgrade head succeeds from migration 0004 through 0007"
    - "tenders table has source TEXT NOT NULL DEFAULT 'goszakup', region TEXT nullable, spgz_code TEXT nullable columns"
    - "client_filters table has UNIQUE(user_id) constraint"
    - "tender_matches table has UNIQUE(user_id, tender_id) and indexes on user_id and status"
    - "ClientFilter and TenderMatch ORM models import cleanly from app.models"
    - "ClientFilterCreate validator rejects keyword lists longer than 20 items"
  artifacts:
    - path: backend/alembic/versions/0005_extend_tenders_source_fields.py
      provides: "ALTER TABLE tenders ADD COLUMN source/region/spgz_code"
    - path: backend/alembic/versions/0006_create_client_filters.py
      provides: "CREATE TABLE client_filters with UNIQUE(user_id)"
    - path: backend/alembic/versions/0007_create_tender_matches.py
      provides: "CREATE TABLE tender_matches with UNIQUE(user_id, tender_id) and status indexes"
    - path: backend/app/models/client_filter.py
      provides: "ClientFilter ORM model"
      exports: ["ClientFilter"]
    - path: backend/app/models/tender_match.py
      provides: "TenderMatch ORM model"
      exports: ["TenderMatch"]
    - path: backend/app/schemas/client_filter.py
      provides: "Pydantic schemas for filter CRUD"
      exports: ["ClientFilterCreate", "ClientFilterResponse"]
    - path: backend/app/schemas/tender_match.py
      provides: "Pydantic schema for discovery feed"
      exports: ["TenderMatchResponse"]
  key_links:
    - from: backend/app/models/tender_match.py
      to: backend/app/models/tender.py
      via: "ForeignKey('tenders.id', ondelete='CASCADE')"
      pattern: "ForeignKey.*tenders"
    - from: backend/app/models/client_filter.py
      to: backend/app/models/user.py
      via: "ForeignKey('users.id', ondelete='CASCADE')"
      pattern: "ForeignKey.*users"
---

<objective>
Lay the DB foundation for Phase 7 discovery: three Alembic migrations that extend the tenders table and create client_filters + tender_matches tables, four ORM models (including extending Tender), and two Pydantic schema files.

Purpose: Every subsequent plan in Phase 7 (workers, matching, CRUD, Telegram, frontend) imports from these models and schemas. This plan has no upstream dependencies and runs in parallel with 07-02.

Output: Applied migrations 0005-0007, updated Tender ORM, new ClientFilter + TenderMatch ORMs, new ClientFilterCreate/Response + TenderMatchResponse Pydantic schemas.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-discovery-matching/07-CONTEXT.md
@.planning/phases/07-discovery-matching/07-RESEARCH.md

<interfaces>
From backend/app/models/tender.py (existing columns — DO NOT drop or rename):
  id, number_anno (UNIQUE), name_ru, name_kz, total_sum, customer_name_ru,
  customer_name_kz, status_id, status_name_ru, start_date, end_date (= submission deadline),
  publish_date, lots_data (JSONB), raw_data (JSONB), cached_at, created_at
  NOTE: end_date IS the submission deadline. Do NOT add deadline_at column (RESEARCH pitfall 4).
  NOTE: raw_data already exists. Do NOT rename to raw_payload (RESEARCH section).

From backend/alembic/versions/ — last migration is 0004_create_applications.py.
Next migration numbers: 0005, 0006, 0007.

From backend/app/models/user.py — users table primary key is Integer id.
From backend/app/models/application.py — pattern for mapping status fields.

Locked decisions from 07-CONTEXT.md:
  D-01: No zakup.sk.kz — do NOT add UNIQUE(source, external_number). Existing UNIQUE(number_anno) is sufficient.
  D-02: No profitability column in tender_matches.
  D-10: UNIQUE(user_id) on client_filters — one filter set per user.
  D-11: tender_matches status values: 'matched' | 'notified' | 'skipped' | 'participating'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migrations 0005-0007 and extend Tender ORM</name>
  <files>
    backend/alembic/versions/0005_extend_tenders_source_fields.py,
    backend/alembic/versions/0006_create_client_filters.py,
    backend/alembic/versions/0007_create_tender_matches.py,
    backend/app/models/tender.py
  </files>
  <read_first>
    backend/alembic/versions/0004_create_applications.py (revision chain — copy down_revision pattern),
    backend/app/models/tender.py (existing columns — add after created_at)
  </read_first>
  <action>
Create three Alembic migration files and extend the Tender ORM model.

Migration 0005 (depends_on: 0004):
- op.add_column('tenders', sa.Column('source', sa.Text(), nullable=False, server_default='goszakup'))
- op.add_column('tenders', sa.Column('region', sa.Text(), nullable=True))
- op.add_column('tenders', sa.Column('spgz_code', sa.Text(), nullable=True))
- downgrade: op.drop_column for all three
- NO new UNIQUE constraint (D-01 locked — existing UNIQUE(number_anno) is sufficient)

Migration 0006 (depends_on: 0005):
- op.create_table('client_filters', ...) with columns:
    id SERIAL PK, user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    keywords ARRAY of Text NOT NULL DEFAULT '{}', spgz_codes ARRAY of Text NOT NULL DEFAULT '{}',
    region TEXT, min_amount NUMERIC(18,2), max_amount NUMERIC(18,2),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
- op.create_unique_constraint('uq_client_filters_user_id', 'client_filters', ['user_id'])
- downgrade: op.drop_table('client_filters')

Migration 0007 (depends_on: 0006):
- op.create_table('tender_matches', ...) with columns:
    id SERIAL PK, user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    tender_id INTEGER NOT NULL REFERENCES tenders(id) ON DELETE CASCADE,
    status TEXT NOT NULL DEFAULT 'matched',
    notified_at TIMESTAMPTZ nullable, decided_at TIMESTAMPTZ nullable,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
- op.create_unique_constraint('uq_tender_matches_user_tender', 'tender_matches', ['user_id', 'tender_id'])
- op.create_index('idx_tender_matches_user_id', 'tender_matches', ['user_id'])
- op.create_index('idx_tender_matches_status', 'tender_matches', ['status'])
- downgrade: drop indexes, drop table
- NO profitability column (D-02 locked)

Extend backend/app/models/tender.py — add three Mapped columns to the Tender class after the existing created_at column:
    source: Mapped[str] = mapped_column(Text, nullable=False, server_default='goszakup')
    region: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    spgz_code: Mapped[Optional[str]] = mapped_column(Text, nullable=True)

Use ARRAY type from sqlalchemy.dialects.postgresql for the keywords/spgz_codes columns in migrations.
In SQLAlchemy ORM models, use ARRAY(Text) from sqlalchemy.dialects.postgresql.

After creating files: cd backend && alembic upgrade head
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && alembic upgrade head 2>&1 | tail -5 && alembic current 2>&1 | grep -c "head"</automated>
  </verify>
  <done>
    alembic current shows revision 0007 (head). psql query confirms tenders.source, client_filters.uq_client_filters_user_id, tender_matches.uq_tender_matches_user_tender all exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: ClientFilter + TenderMatch ORM models and Pydantic schemas</name>
  <files>
    backend/app/models/client_filter.py,
    backend/app/models/tender_match.py,
    backend/app/schemas/client_filter.py,
    backend/app/schemas/tender_match.py
  </files>
  <read_first>
    backend/app/models/application.py (ORM pattern with mapped_column + Mapped),
    backend/app/schemas/application.py (Pydantic v2 schema pattern with model_config)
  </read_first>
  <action>
Create four new files.

backend/app/models/client_filter.py:
- ClientFilter(Base) with __tablename__ = 'client_filters'
- Columns: id (PK), user_id (FK users.id CASCADE), keywords (ARRAY(Text), default=[]),
  spgz_codes (ARRAY(Text), default=[]), region (Text nullable),
  min_amount (Numeric(18,2) nullable), max_amount (Numeric(18,2) nullable),
  created_at, updated_at (both DateTime(timezone=True) with server_default=func.now())
- No relationships needed (only used as a data record)

backend/app/models/tender_match.py:
- TenderMatch(Base) with __tablename__ = 'tender_matches'
- Columns: id (PK), user_id (FK users.id CASCADE), tender_id (FK tenders.id CASCADE),
  status (String(50), nullable=False, default='matched'),
  notified_at (DateTime(timezone=True) nullable), decided_at (DateTime(timezone=True) nullable),
  created_at, updated_at (both DateTime(timezone=True) with server_default=func.now())
- No eager relationships (use lazy="noload" to match Tender/Application pattern)

backend/app/schemas/client_filter.py:
- ClientFilterCreate(BaseModel): keywords: list[str] = [], spgz_codes: list[str] = [],
  region: str | None = None, min_amount: Decimal | None = None, max_amount: Decimal | None = None
  Validator: @field_validator('keywords') — raise ValueError if len > 20 (DoS guard, T-07-04)
- ClientFilterResponse(ClientFilterCreate): id: int, user_id: int, created_at: datetime,
  updated_at: datetime. model_config = ConfigDict(from_attributes=True)

backend/app/schemas/tender_match.py:
- TenderMatchResponse(BaseModel): id: int, user_id: int, tender_id: int,
  status: str, notified_at: datetime | None, decided_at: datetime | None, created_at: datetime,
  tender_name_ru: str | None = None, customer_name_ru: str | None = None,
  total_sum: Decimal | None = None, end_date: datetime | None = None, region: str | None = None
  (tender fields denormalized for frontend use — populated by JOIN in the discovery router)
  model_config = ConfigDict(from_attributes=True)

Import Decimal from decimal, datetime from datetime, Optional from typing.
Use model_config = ConfigDict(from_attributes=True) on all response schemas (Pydantic v2).
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -c "from app.models.client_filter import ClientFilter; from app.models.tender_match import TenderMatch; from app.schemas.client_filter import ClientFilterCreate, ClientFilterResponse; from app.schemas.tender_match import TenderMatchResponse; print('OK')"</automated>
  </verify>
  <done>
    All four modules import cleanly. ClientFilterCreate raises ValueError when keywords list exceeds 20 items. TenderMatchResponse has tender_name_ru, total_sum, end_date, region as nullable fields.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema | Migrations add columns/tables; incorrect defaults or missing constraints could allow data integrity violations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service | ClientFilterCreate.keywords | mitigate | @field_validator rejects keyword lists > 20 items at schema level; parameterized ILIKE prevents SQL injection via SQLAlchemy |
| T-07-schema-01 | Tampering | tender_matches UNIQUE | mitigate | UNIQUE(user_id, tender_id) constraint prevents duplicate match records even under concurrent ARQ runs (Pitfall 3 in RESEARCH.md) |
| T-07-schema-02 | Spoofing | client_filters.user_id | mitigate | ON DELETE CASCADE from users(id) ensures filter records cannot outlive their owner |
</threat_model>

<verification>
After both tasks complete:
1. `cd backend && alembic current` shows "(head)" next to revision 0007
2. `python -c "from app.models.client_filter import ClientFilter; from app.models.tender_match import TenderMatch; print('models OK')"` — no ImportError
3. `python -c "from app.schemas.client_filter import ClientFilterCreate; ClientFilterCreate(keywords=['a']*21)"` — raises ValidationError
4. `cd backend && pytest tests/test_health.py -x` — existing tests still pass (no regression from model changes)
</verification>

<success_criteria>
- alembic upgrade head succeeds from 0004 to 0007 without errors
- tenders table gains source (NOT NULL DEFAULT 'goszakup'), region (nullable), spgz_code (nullable)
- client_filters table exists with UNIQUE(user_id); tender_matches with UNIQUE(user_id, tender_id) and two indexes
- All four new Python files import cleanly in the FastAPI app context
- ClientFilterCreate.keywords validator enforces max 20 items
- No existing tests broken (run pytest tests/ -x after migration)
</success_criteria>

<output>
After completion, create .planning/phases/07-discovery-matching/07-01-SUMMARY.md
</output>
