---
phase: 07-discovery-matching
plan: 06
type: execute
wave: 4
depends_on:
  - "07-05"
files_modified: []
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06

must_haves:
  truths:
    - "All 6 DISC requirements verified by human"
    - "Backend tests pass (matching, filters, matches, telegram webhook, application service)"
    - "Frontend pages compile and render correctly"
    - "ARQ worker registered with new cron and functions"
  artifacts:
    - path: .planning/phases/07-discovery-matching/07-06-SUMMARY.md
      provides: "Phase 7 completion record"
  key_links: []
---

<objective>
Human verification checkpoint: confirm that all Phase 7 discovery & matching functionality works end-to-end before marking the phase complete.

Purpose: Automated tests cover unit and integration logic; this checkpoint catches visual, UX, and integration issues that tests cannot catch — especially the Sidebar links, empty-state copy, and filter form UX.

Output: Human confirmation that Phase 7 is functionally complete.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-discovery-matching/07-CONTEXT.md
@.planning/phases/07-discovery-matching/07-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 7 end-to-end verification</name>
  <what-built>
    - DB migrations 0005-0007 applied (tenders extended, client_filters, tender_matches tables)
    - ARQ cron poll_goszakup_discovery (every 15 min) + run_matching (on-demand) registered
    - Matching service (ILIKE keywords, region, spgz_code, amount range)
    - Discovery CRUD API (/api/discovery/filters, /api/discovery/matches, participate, skip)
    - Telegram disc:* callback handlers with IDOR protection
    - create_discovery_draft (bypasses Pydantic validator)
    - Frontend: /discovery feed page + /discovery-filters settings page
    - TenderMatchCard + TenderMatchStatusBadge components
    - Sidebar: "Подборка" nav + "Telegram бот" external link (D-08)
    - middleware.ts: /discovery and /discovery-filters protected
  </what-built>
  <how-to-verify>
    Run these checks in order:

    1. BACKEND TESTS — all must pass:
       cd backend && pytest tests/test_matching_service.py tests/test_discovery_filters.py tests/test_discovery_matches.py tests/test_application_service.py tests/test_telegram_disc_webhook.py tests/test_goszakup_batch.py tests/test_poll_discovery.py -x -v

    2. MIGRATION CHECK:
       cd backend && alembic current
       Expected: shows "(head)" next to revision 0007

    3. WORKER SETTINGS CHECK:
       grep -n "poll_goszakup_discovery\|run_matching" backend/app/workers/worker_settings.py
       Expected: poll_goszakup_discovery in cron_jobs (minute={0,15,30,45}); run_matching in functions list

    4. MIDDLEWARE CHECK:
       grep "discovery" frontend/src/middleware.ts
       Expected: '/discovery' and '/discovery-filters' in protectedRoutes

    5. FRONTEND: Start the frontend dev server and visit:
       a. http://localhost:3000/discovery — WITHOUT logging in → should redirect to /login
       b. http://localhost:3000/discovery-filters — WITHOUT logging in → should redirect to /login
       c. Log in → visit http://localhost:3000/discovery → page loads, shows empty state with "Настройте фильтры" link
       d. Click "Настройте фильтры" or visit http://localhost:3000/discovery-filters
       e. Enter keywords: "строительство", click "Сохранить" → see "Фильтры сохранены"
       f. Reload /discovery-filters → keywords field pre-filled with "строительство"
       g. Sidebar: confirm "Подборка" nav item is visible with an icon
       h. Sidebar bottom section: confirm "Telegram бот" link is visible (need NEXT_PUBLIC_TELEGRAM_BOT_USERNAME in .env.local)

    6. DISCOVERY FEED (requires a match record):
       Insert a test match manually via psql or Django shell:
         INSERT INTO tender_matches (user_id, tender_id, status) VALUES (1, 1, 'matched');
       Reload /discovery → TenderMatchCard appears with "Новый" status badge
       Click "Пропустить" → card updates to "Пропущен" (or refreshes)

    7. TELEGRAM NOTIFICATIONS (requires telegram_chat_id set on user):
       If user has telegram_chat_id configured, trigger run_matching manually:
         cd backend && python -c "import asyncio; from app.workers.tasks.run_matching import run_matching; ..."
       Or wait for next poll cycle (15 min).
       Verify: Telegram message arrives with "Участвуем" and "Пропустить" inline buttons.

    8. REGRESSION CHECK — existing tests:
       cd backend && pytest tests/test_telegram_webhook.py tests/test_applications.py -x
       Expected: all pass (no regression from Phase 5 confirm:* flow)
  </how-to-verify>
  <resume-signal>
    Type "approved" when all checks pass, or describe specific issues found (e.g., "step 5c fails: page shows 500 error — matching_service import fails").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verifier | Checkpoint; no new code paths introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | POST /api/discovery/{match_id}/participate | verify | Manual IDOR test: attempt participate with another user's match_id → confirm 404 |
| T-07-02 | Elevation of Privilege | POST /api/discovery/{match_id}/skip | verify | Same IDOR manual test for skip |
| T-07-03 | Information Disclosure | GET /api/discovery/matches | verify | Confirm GET returns only the logged-in user's matches |
| T-07-04 | Denial of Service | poll_goszakup_discovery | verify | Confirm asyncio.sleep(0.5) comment in poll worker code; cron unique=True prevents overlap |
| T-07-05 | Spoofing | Telegram disc:* handlers | verify | Confirm secret token check (existing T-05-31) applies; IDOR chat_id check in place |
</threat_model>

<verification>
All checks listed in how-to-verify above must pass before approving.
</verification>

<success_criteria>
- All backend tests in Phase 7 pass
- /discovery and /discovery-filters redirect unauthenticated users to /login
- Discovery feed page renders correctly (empty state or match cards)
- Filter settings page saves and pre-fills correctly
- Sidebar shows both "Подборка" nav and "Telegram бот" external link
- ARQ worker_settings.py has poll_goszakup_discovery cron and run_matching in functions
- No regression in existing Phase 5 telegram confirm:* tests
- Human types "approved" to close the checkpoint
</success_criteria>

<output>
After checkpoint passes, create .planning/phases/07-discovery-matching/07-06-SUMMARY.md
</output>
