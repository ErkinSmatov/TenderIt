---
phase: 3
slug: tender-lookup
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-10
---

# Phase 3 — UI Design Contract: Tender Lookup

> Visual and interaction contract for Phase 3 frontend. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — raw Tailwind CSS (established in Phase 2) |
| Icon library | none (Phase 2 established no icon library) |
| Font | system stack — Arial, Helvetica, sans-serif (globals.css) |

**Note on shadcn:** shadcn is not initialized and was not used in Phase 2. Introducing it now would create a split design system (shadcn components alongside raw Tailwind forms). Decision: continue with the established raw Tailwind pattern for consistency. Shadcn can be adopted at a natural boundary (e.g. Phase 5 or a dedicated design-system upgrade phase).

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Tailwind Class | Usage |
|-------|-------|---------------|-------|
| xs | 4px | `gap-1`, `p-1` | Icon gaps, tight inline padding |
| sm | 8px | `gap-2`, `p-2` | Compact element spacing, lot list items |
| md | 16px | `gap-4`, `p-4` | Default element spacing, form field gaps |
| lg | 24px | `gap-6`, `p-6` | Section padding (matches existing `main p-6`) |
| xl | 32px | `gap-8`, `mb-8` | Between major sections on tender card |
| 2xl | 48px | `py-12` | Empty/error state vertical centering |
| 3xl | 64px | — | Not used in this phase |

Exceptions: none

---

## Typography

| Role | Size | Tailwind | Weight | Weight Class | Line Height |
|------|------|----------|--------|--------------|-------------|
| Body | 14px | `text-sm` | 400 regular | `font-normal` | 1.5 |
| Label | 14px | `text-sm` | 500 medium | `font-medium` | 1.25 |
| Heading | 20px | `text-xl` | 600 semibold | `font-semibold` | 1.3 |
| Display | 24px | `text-2xl` | 700 bold | `font-bold` | 1.2 |

**Usage rules:**
- Display (24px bold): page `<h1>` only — "Поиск тендера", "Личный кабинет"
- Heading (20px semibold): section headings — tender card title (`nameRu`), watchlist section heading
- Label (14px medium): form labels, field captions, lot numbers, metadata labels
- Body (14px regular): all descriptive text, lot descriptions, customer name, dates

Source: inferred from existing Phase 2 usage — `text-2xl font-bold` for page h1, `text-sm font-medium` for labels, `text-sm` for error/helper text.

---

## Color

| Role | Value | Tailwind Class | Usage |
|------|-------|---------------|-------|
| Dominant (60%) | #ffffff | `bg-white` | Page background, form surfaces |
| Secondary (30%) | #f9fafb | `bg-gray-50` | Tender card background, watchlist rows, lot list items |
| Accent (10%) | #2563eb | `bg-blue-600` | Reserved list below |
| Destructive | #ef4444 | `text-red-500` / `bg-red-50` | Error messages, validation errors only |

Accent (`bg-blue-600` / `text-blue-600`) reserved for:
1. Primary CTA button ("Найти")
2. Watchlist "Добавить в список" button (default state)
3. Navigation links in dashboard nav (`text-blue-600 hover:underline` — existing pattern)

Accent is NOT used for: card borders, lot items, informational badges, loading states, metadata text.

**Status badge colors (tender `statusNameRu`):**
- "Принимаются заявки" (open): `bg-green-50 text-green-700` — green badge
- "Завершен" / "Отменен": `bg-gray-100 text-gray-600` — gray badge
- All other statuses: `bg-yellow-50 text-yellow-700` — amber badge
- Badge shape: `rounded-full px-2 py-0.5 text-xs font-medium`

**Watchlist button states:**
- Not in watchlist: `bg-blue-600 text-white` — "Добавить в список"
- In watchlist (active): `bg-gray-100 text-gray-700 border border-gray-300` — "В списке"
- Loading (optimistic): `opacity-50 cursor-not-allowed`

Source: matches established Phase 2 color vocabulary (`bg-blue-600`, `text-red-500`, `text-green-600`, `text-gray-600`).

---

## Component Inventory

### 1. Search Page `/tenders` — `TendersPage`

**Layout:** Full-width within `<main className="p-6">`. Max content width `max-w-2xl`.

**Structure:**
```
<h1> Поиск тендера              ← Display (text-2xl font-bold mb-6)
<form>
  <label> Номер объявления      ← Label (text-sm font-medium mb-1 block)
  <input>                       ← border rounded px-3 py-2 w-full (matches existing pattern)
  <p> validation error          ← text-red-500 text-sm mt-1 (matches existing)
  <button> Найти / Поиск...     ← bg-blue-600 text-white rounded px-4 py-2 disabled:opacity-50
<div> result area
  [loading spinner] OR [TenderCard] OR [not-found message]
```

**Input field:** `type="text"`, `placeholder="Например: 123456"`, full width.

**Loading state:** Spinner inline — `<svg>` 16×16 animate-spin in `text-blue-600`, placed left of "Поиск..." button label text. Button remains disabled during loading.

**No result yet (initial state):** Empty result area — nothing rendered below the form.

### 2. Tender Card — `TenderCard`

**Container:** `bg-gray-50 border border-gray-200 rounded-lg p-6 mt-6`

**Field layout — two-column grid on md+, single column on mobile:**
```
Название тендера                      ← Heading (text-xl font-semibold mb-4), full width
────────────────────────────────────────
Заказчик        [customerNameRu]       ← label + value pair
Номер объявления [numberAnno]          ← label + value pair
Сумма           [totalSum formatted]   ← label + value pair (formatted as KZT)
Дедлайн         [endDate formatted]    ← label + value pair (DD.MM.YYYY HH:MM)
Статус          [StatusBadge]          ← label + badge component
────────────────────────────────────────
Лоты ([countLots])                     ← section heading (text-sm font-medium text-gray-500 mb-2)
  Лот 1  [lotNumber]  [nameRu]  [amount KZT]   ← lot list items
  ...
────────────────────────────────────────
[WatchlistButton]                      ← full width on mobile, auto width on md+
```

**Label style:** `text-sm font-medium text-gray-500` (muted label)
**Value style:** `text-sm text-gray-900`
**totalSum format:** `Intl.NumberFormat('ru-KZ', { style: 'currency', currency: 'KZT', maximumFractionDigits: 0 })` — example: "1 500 000 ₸"
**endDate format:** `DD.MM.YYYY` — example: "30.06.2026". Show "—" if null.

**Lot list item:** `flex justify-between items-center py-2 border-b border-gray-100 last:border-0`
- Left: `text-sm text-gray-900` lot name (`nameRu`)
- Right: `text-sm font-medium text-gray-700` lot amount (formatted KZT)
- Lot number prefix: `text-xs text-gray-400 mr-2` — "Лот 1", "Лот 2"

### 3. Watchlist Button — `WatchlistButton`

States:
- **Default (not watching):** `bg-blue-600 text-white rounded px-4 py-2 text-sm font-medium hover:bg-blue-700`
  Label: "Добавить в список"
- **Active (watching):** `bg-gray-100 text-gray-700 border border-gray-300 rounded px-4 py-2 text-sm font-medium hover:bg-red-50 hover:text-red-600 hover:border-red-300`
  Label: "В списке" (hover changes to imply "Удалить")
- **Loading:** add `opacity-50 cursor-not-allowed` to current state
- **Transition:** no CSS transition — state change is instant after API response

### 4. Dashboard Watchlist Section

Added to `/dashboard` page below existing nav links.

**Section heading:** `<h2 className="text-xl font-semibold mb-4 mt-8">Отслеживаемые тендеры</h2>`

**Watchlist rows:** `bg-gray-50 border border-gray-200 rounded-lg divide-y divide-gray-200`

**Per-row layout:** `flex items-center justify-between px-4 py-3`
- Left: tender title `text-sm font-medium text-gray-900` + number_anno `text-xs text-gray-500 ml-2`
- Right: `StatusBadge` + `WatchlistButton` (remove variant, compact: `text-xs px-2 py-1`)

**Empty watchlist:** see Copywriting Contract below.

---

## Interaction Contract

### Search Flow

1. User types numberAnno → no live validation (validates only on submit)
2. User presses Enter or clicks "Найти"
3. Button becomes `disabled`, label changes to "Поиск..."
4. Spinner appears
5a. Success (200): spinner disappears, `TenderCard` mounts with slide-in — `transition-all duration-200`
5b. Not found (404): spinner disappears, not-found message appears (see Copywriting)
5c. Network error: error message appears (see Copywriting)
6. User can modify the input and search again — previous result replaces with new result

### Watchlist Toggle Flow

1. User clicks "Добавить в список"
2. Button switches to loading state immediately (optimistic)
3a. API 201: button switches to "В списке" state
3b. API error: button reverts to "Добавить в список", inline error shown below button: `text-red-500 text-sm mt-1`

### Validation Rules (frontend)

| Field | Rule | Error message |
|-------|------|---------------|
| numberAnno (search) | min 1 char, max 100 chars, strip whitespace | "Введите номер объявления" |

No regex validation — exact format confirmed in Wave 0 spike (CONTEXT.md decision).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Search page heading | Поиск тендера |
| Search input label | Номер объявления |
| Search input placeholder | Например: 123456 |
| Primary CTA (search) | Найти |
| Loading state button | Поиск... |
| Not found heading | Тендер не найден |
| Not found body | Тендер с номером {ID} не найден на портале goszakup.gov.kz. Проверьте номер и попробуйте снова. |
| Network error | Не удалось загрузить данные. Проверьте соединение и попробуйте ещё раз. |
| Watchlist section heading (dashboard) | Отслеживаемые тендеры |
| Watchlist empty state heading | Список пуст |
| Watchlist empty state body | Найдите тендер и нажмите «Добавить в список». |
| Watchlist add CTA | Добавить в список |
| Watchlist active state | В списке |
| Watchlist remove confirm (hover hint) | Кнопка меняет цвет на красный при hover — нет отдельного диалога подтверждения для удаления из watchlist |
| Watchlist remove error | Не удалось удалить из списка. Попробуйте снова. |
| Lot section label | Лоты ({n}) |
| Status field label | Статус |
| Customer field label | Заказчик |
| Amount field label | Сумма |
| Deadline field label | Дедлайн |
| Announcement number label | Номер объявления |

**Destructive actions in this phase:** Removing a tender from watchlist.
Confirmation approach: NO modal confirmation. The button hover state signals intent (turns red). Removal is reversible — user can immediately add the tender back.

---

## States Coverage

| Component | States |
|-----------|--------|
| TendersPage | idle, loading, success (tender found), not-found, error |
| TenderCard | default display, watchlist-loading |
| WatchlistButton | not-watching, watching, loading |
| Dashboard watchlist | empty, populated (1+ items), loading |

---

## Accessibility Contract

- All interactive elements have visible focus ring via Tailwind `focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-1`
- Search input has explicit `<label>` with `htmlFor` (matches existing pattern)
- WatchlistButton: `aria-pressed={isWatching}`, `aria-label` changes with state
- StatusBadge: rendered as `<span>` with no interactive role
- Loading spinner: `role="status"` with `<span className="sr-only">Загрузка...</span>`
- Lot list: rendered as `<ul>` / `<li>` (semantic list)
- Tender card: rendered as `<article>` (landmark for assistive technology)
- Min touch target for buttons: 40px height via `py-2` (32px) minimum — OK for desktop; phase is desktop-first

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (shadcn not initialized) |
| third-party | none | not applicable |

No third-party component blocks in this phase. All UI is hand-authored Tailwind following existing Phase 2 patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
