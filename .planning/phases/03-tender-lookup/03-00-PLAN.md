---
phase: 03-tender-lookup
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/tests/spikes/test_spike01_goszakup.py
  - backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md
  - backend/app/services/goszakup_service.py
  - backend/tests/test_tender_service.py
  - backend/tests/test_tenders.py
  - frontend/src/app/(dashboard)/Providers.tsx
  - frontend/src/app/(dashboard)/layout.tsx
autonomous: false
requirements:
  - SRCH-02
must_haves:
  truths:
    - "Wave 0 spike confirms the goszakup token works against the live GraphQL endpoint and records the real numberAnno string format and date string format in SPIKE-01-GRAPHQL-FINDINGS.md"
    - "The refBuyStatusId integer value for 'open for applications' (Принимаются заявки) status is recorded in SPIKE-01-GRAPHQL-FINDINGS.md (Wave 1 gate)"
    - "respx==0.23.1 is present in pyproject.toml [project.optional-dependencies] dev"
    - "Test scaffold files test_tender_service.py and test_tenders.py exist and import cleanly (red, not error) so Wave 1-2 have a target"
    - "goszakup_service.py stub exists with fetch_tender_by_number_anno signature so test imports resolve"
    - "QueryClientProvider wraps the dashboard route group so Wave 3 useQuery calls do not throw"
  artifacts:
    - path: "backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md"
      provides: "Recorded real numberAnno format, date format, and refBuyStatusId open-status value"
    - path: "backend/app/services/goszakup_service.py"
      provides: "fetch_tender_by_number_anno stub (raises NotImplementedError)"
    - path: "backend/tests/test_tender_service.py"
      provides: "Wave 1 service unit test scaffold (respx mocks)"
    - path: "backend/tests/test_tenders.py"
      provides: "Wave 2 route integration test scaffold"
    - path: "frontend/src/app/(dashboard)/Providers.tsx"
      provides: "QueryClientProvider client wrapper"
  key_links:
    - from: "backend/tests/spikes/test_spike01_goszakup.py"
      to: "https://ows.goszakup.gov.kz/v3/graphql"
      via: "live TrdBuy query with Bearer token, skipif HAS_TOKEN"
      pattern: "TrdBuy.*numberAnno"
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "Providers (QueryClientProvider)"
      via: "client component wrapper around children"
      pattern: "Providers"
---

<objective>
Wave 0 — narrow targeted spike plus test scaffolding. The API contract is fully known
(03-CONTEXT.md). This wave resolves the three remaining unknowns and lays down the test
targets that Wave 1-2 implement against.

Resolves:
- Confirm goszakup token works against the live GraphQL endpoint (SRCH-02)
- Record the real `numberAnno` string format (is it "123456" or prefixed?)
- Record the real date string format from `startDate`/`endDate`/`publishDate`
- Record the `refBuyStatusId` integer value that means "open for applications" — THIS GATES WAVE 1

Also scaffolds (so Wave 1-3 have concrete red targets):
- `respx==0.23.1` dev dependency
- `goszakup_service.py` stub
- `test_tender_service.py` + `test_tenders.py` red test scaffolds
- `QueryClientProvider` wrapper for the dashboard route group

Purpose: De-risk the live API and unblock all downstream waves with a single gate.
Output: SPIKE findings doc, updated spike test, dev dep, two test scaffolds, service stub, frontend provider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tender-lookup/03-CONTEXT.md
@.planning/phases/03-tender-lookup/03-RESEARCH.md
@.planning/phases/03-tender-lookup/03-VALIDATION.md
@backend/tests/spikes/test_spike01_goszakup.py
@backend/pyproject.toml
@frontend/src/app/(dashboard)/layout.tsx

<interfaces>
goszakup GraphQL endpoint: POST https://ows.goszakup.gov.kz/v3/graphql
Auth header: Authorization: Bearer {settings.goszakup_api_token}
TrdBuy query (full): see 03-CONTEXT.md "GraphQL query for tender lookup"
Not-found shape: {"data": {"TrdBuy": []}} at HTTP 200

Existing spike test already defines: GRAPHQL_URL, GOSZAKUP_TOKEN, HAS_TOKEN, _auth_headers(),
and skipif(not HAS_TOKEN). Reuse these — do not redefine.

Service stub signature (Wave 1 will implement):
  async def fetch_tender_by_number_anno(number_anno: str) -> dict | None
</interfaces>
</context>

<tasks>

<task id="03-00-01" type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live goszakup spike — record numberAnno, date format, and open-status code</name>
  <read_first>
    - backend/tests/spikes/test_spike01_goszakup.py — existing GRAPHQL_URL, HAS_TOKEN, _auth_headers, skipif pattern to reuse
    - .planning/phases/03-tender-lookup/03-CONTEXT.md — "Wave 0 — Targeted Spike" section and full TrdBuy query
    - .planning/phases/03-tender-lookup/03-RESEARCH.md — Open Questions 1-4 (status code, numberAnno format, dates)
  </read_first>
  <action>
    Extend backend/tests/spikes/test_spike01_goszakup.py with a new test `test_trdbuy_query_returns_tender` that:
    - Reuses existing GRAPHQL_URL, _auth_headers(), and @pytest.mark.skipif(not HAS_TOKEN) decorators
    - POSTs the full TrdBuy query from 03-CONTEXT.md with variables {"numberAnno": "<real id provided by developer at runtime via env var TEST_TENDER_NUMBER, default to a known-public tender id>"}
    - Asserts HTTP 200 and that response body has key path data.TrdBuy
    - Does NOT print or log the Bearer token (CLAUDE.md directive)
    - Writes the redacted response (token never appears) to backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md via the developer running the test

    Create backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md with these required sections (developer fills runtime values):
    - "## Token Status" — confirmed working / failing
    - "## Real numberAnno Format" — the exact string returned (e.g. "123456" vs "RU-2025-...")
    - "## Date String Format" — raw startDate/endDate/publishDate strings (ISO-8601? unix? tz?)
    - "## refBuyStatusId Reference" — table of refBuyStatusId integer → RefBuyStatus.nameRu, with the value meaning "Принимаются заявки" (open for applications) explicitly marked. THIS VALUE GATES WAVE 1 — Phase 5 polling depends on it.
    - "## Redacted Sample Response" — full TrdBuy JSON with customerBin and any PII redacted

    This is a human-action checkpoint because it requires a live GOSZAKUP_API_TOKEN that only the developer holds, run against production (no sandbox exists — STATE.md).
  </action>
  <how-to-verify>
    1. Ensure GOSZAKUP_API_TOKEN is exported in your shell (1-year token, obtained 2026-06-09)
    2. Run: `cd backend && GOSZAKUP_API_TOKEN=$GOSZAKUP_API_TOKEN TEST_TENDER_NUMBER=<a real open tender number> pytest tests/spikes/test_spike01_goszakup.py -v`
    3. Confirm test_trdbuy_query_returns_tender PASSES (not skipped)
    4. Open backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md and confirm all five sections are filled, especially the refBuyStatusId value for "Принимаются заявки"
  </how-to-verify>
  <acceptance_criteria>
    - Source: `grep -c "test_trdbuy_query_returns_tender" backend/tests/spikes/test_spike01_goszakup.py` returns 1
    - Source: `grep -c "TrdBuy" backend/tests/spikes/test_spike01_goszakup.py` returns 1 or more
    - File exists: backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md
    - Source: `grep -ci "refBuyStatusId" backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md` returns 1 or more
    - Source: `grep -ci "Принимаются заявки" backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md` returns 1 or more
    - Behavior: token value never appears in SPIKE-01-GRAPHQL-FINDINGS.md (`grep -c "Bearer" backend/spikes/findings/SPIKE-01-GRAPHQL-FINDINGS.md` returns 0)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the findings doc has the open-status refBuyStatusId value recorded, or describe what is still unknown.</resume-signal>
</task>

<task id="03-00-02" type="auto">
  <name>Task 2: Add respx dev dep, goszakup_service stub, and red test scaffolds</name>
  <files>
    backend/pyproject.toml,
    backend/app/services/goszakup_service.py,
    backend/tests/test_tender_service.py,
    backend/tests/test_tenders.py
  </files>
  <read_first>
    - backend/pyproject.toml — [project.optional-dependencies] dev block (currently: pytest, pytest-asyncio, httpx, fakeredis)
    - backend/tests/conftest.py — existing fixtures (db session, authed client pattern from Phase 2)
    - .planning/phases/03-tender-lookup/03-RESEARCH.md — Pattern 1 (service signature), Pattern 8 (respx mocks), Validation Architecture test map
    - .planning/phases/03-tender-lookup/03-VALIDATION.md — Per-Task Verification Map (exact test names: test_fetch_tender_found, test_cache_hit_skips_api, test_cache_stale_refetches, test_fetch_tender_not_found, test_get_tender_found, test_get_tender_not_found, test_add_to_watchlist, test_add_watchlist_idempotent, test_remove_from_watchlist, test_get_watchlist)
  </read_first>
  <action>
    Edit backend/pyproject.toml: add "respx==0.23.1" to the [project.optional-dependencies] dev list.

    Create backend/app/services/goszakup_service.py as a stub so test imports resolve:
    - module constants GRAPHQL_URL = "https://ows.goszakup.gov.kz/v3/graphql" and TENDER_QUERY (the full query string from 03-RESEARCH.md Pattern 1)
    - async def fetch_tender_by_number_anno(number_anno: str) -> dict | None: raise NotImplementedError("Wave 1: 03-01")
    Do NOT hardcode the token. Do not import settings yet beyond what is needed for the stub (the token field is added in Wave 1).

    Create backend/tests/test_tender_service.py with respx-based unit test stubs (each test body may assert False or pytest.skip with reason="Wave 1 implements" so they collect as red, NOT error). Define exactly these test function names so Wave 1 fills them:
    - test_fetch_tender_found
    - test_fetch_tender_not_found
    - test_cache_hit_skips_api
    - test_cache_stale_refetches
    - test_tender_response_schema
    Import respx and httpx at top (this validates respx is installed). Use GRAPHQL_URL = "https://ows.goszakup.gov.kz/v3/graphql".

    Create backend/tests/test_tenders.py with route integration test stubs (skip with reason="Wave 2 implements"). Define exactly these names:
    - test_get_tender_found
    - test_get_tender_not_found
    - test_add_to_watchlist
    - test_add_watchlist_idempotent
    - test_remove_from_watchlist
    - test_get_watchlist
    Reuse the authed-client fixture from conftest.py (Phase 2 pattern).

    Install the new dep so imports work: `cd backend && pip install respx==0.23.1`.
  </action>
  <verify>
    <automated>cd backend && pip install respx==0.23.1 -q && pytest tests/test_tender_service.py tests/test_tenders.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "respx==0.23.1" backend/pyproject.toml` returns 1
    - Source: `grep -c "async def fetch_tender_by_number_anno" backend/app/services/goszakup_service.py` returns 1
    - Source: `grep -c "GRAPHQL_URL" backend/app/services/goszakup_service.py` returns 1 or more
    - Behavior: `cd backend && pytest tests/test_tender_service.py tests/test_tenders.py --collect-only -q` exits 0 and collects 11 test functions (5 service + 6 route)
    - Source: `grep -E "def test_(fetch_tender_found|cache_hit_skips_api|cache_stale_refetches|fetch_tender_not_found|tender_response_schema)" backend/tests/test_tender_service.py | wc -l` returns 5
    - Source: `grep -E "def test_(get_tender_found|get_tender_not_found|add_to_watchlist|add_watchlist_idempotent|remove_from_watchlist|get_watchlist)" backend/tests/test_tenders.py | wc -l` returns 6
    - Source: `grep -c "import respx" backend/tests/test_tender_service.py` returns 1
  </acceptance_criteria>
  <done>
    respx is a dev dep and installed; service stub and both test scaffolds collect cleanly as red (skipped), not errored; all 11 named test functions exist.
  </done>
</task>

<task id="03-00-03" type="auto">
  <name>Task 3: Add QueryClientProvider wrapper to dashboard route group</name>
  <files>
    frontend/src/app/(dashboard)/Providers.tsx,
    frontend/src/app/(dashboard)/layout.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/layout.tsx — current server-component layout (no 'use client'); QueryClientProvider must be a separate client component imported here
    - frontend/package.json — confirm @tanstack/react-query@5.100.14 present
    - .planning/phases/03-tender-lookup/03-RESEARCH.md — Pitfall 6 (QueryClientProvider scope: dashboard route group, not global)
  </read_first>
  <action>
    Create frontend/src/app/(dashboard)/Providers.tsx as a 'use client' component:
    - import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
    - create a stable QueryClient instance via useState(() => new QueryClient({ defaultOptions: { queries: { retry: false, staleTime: 60000 } } }))
    - export default function Providers({ children }: { children: ReactNode }) returning <QueryClientProvider client={client}>{children}</QueryClientProvider>

    Edit frontend/src/app/(dashboard)/layout.tsx: keep it a server component. Import Providers and wrap {children} inside <Providers>{children}</Providers> within the existing <main className="p-6"> so all dashboard-group pages (dashboard, profile, tenders) get react-query context. Do NOT add 'use client' to layout.tsx — only the Providers child is a client boundary.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8 | grep -E "(error|Error|Failed)" && echo "BUILD_FAIL" || echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "'use client'" "frontend/src/app/(dashboard)/Providers.tsx"` returns 1
    - Source: `grep -c "QueryClientProvider" "frontend/src/app/(dashboard)/Providers.tsx"` returns 1 or more
    - Source: `grep -c "Providers" "frontend/src/app/(dashboard)/layout.tsx"` returns 1 or more
    - Source: `grep -c "'use client'" "frontend/src/app/(dashboard)/layout.tsx"` returns 0 (layout stays server component)
    - Behavior: `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Dashboard route group is wrapped in QueryClientProvider via a client child; layout remains a server component; frontend builds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → goszakup API | Outbound Bearer-authenticated GraphQL call; token is a secret |
| developer shell → findings doc | Live token used at runtime, must never be persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Information Disclosure | goszakup token leaking into spike test output or findings doc | mitigate | Test never prints token; findings doc grep-gated to contain no "Bearer"; token read only from env at runtime |
| T-03-00-02 | Information Disclosure | customerBin / PII in redacted sample response | mitigate | Findings doc instructs redaction of customerBin and PII before commit |
</threat_model>

<verification>
- `cd backend && pytest tests/test_tender_service.py tests/test_tenders.py --collect-only -q` exits 0 (11 collected)
- `cd backend && GOSZAKUP_API_TOKEN=<token> pytest tests/spikes/test_spike01_goszakup.py -v` — TrdBuy test passes (manual, requires token)
- `cd frontend && npm run build` exits 0
- SPIKE-01-GRAPHQL-FINDINGS.md has the open-status refBuyStatusId recorded
</verification>

<success_criteria>
- Wave 1 gate cleared: open-status refBuyStatusId value, real numberAnno format, and date format documented
- respx installed and in pyproject dev deps
- 11 red test scaffolds collect cleanly
- QueryClientProvider in place for Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/03-tender-lookup/03-00-SUMMARY.md` recording:
- Real numberAnno format and date format discovered
- The refBuyStatusId value for "Принимаются заявки" (open)
- Test scaffold names created
- Any surprises from the live API
</output>
