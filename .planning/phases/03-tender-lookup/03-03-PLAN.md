---
phase: 03-tender-lookup
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/types/tender.ts
  - frontend/src/components/tenders/StatusBadge.tsx
  - frontend/src/components/tenders/TenderCard.tsx
  - frontend/src/components/tenders/WatchlistButton.tsx
  - frontend/src/app/(dashboard)/tenders/page.tsx
  - frontend/src/components/tenders/DashboardWatchlist.tsx
  - frontend/src/app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements:
  - SRCH-01
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "User can type a tender number on /tenders, submit, and see a TenderCard with title, customer, amount, deadline, status, and lots (SRCH-01, SRCH-03)"
    - "An unknown tender id shows the 'Тендер не найден' message, not a crash (SRCH-01)"
    - "User can add a tender to the watchlist and the button switches to 'В списке' state (SRCH-04)"
    - "The dashboard shows an 'Отслеживаемые тендеры' section listing watched tenders, with an empty state when none (SRCH-04)"
    - "User can remove a tender from the watchlist and the UI updates"
    - "api.ts has a delete method so DELETE /api/watchlist/{number_anno} can be called"
  artifacts:
    - path: "frontend/src/app/(dashboard)/tenders/page.tsx"
      provides: "Search page (RHF + zod + react-query useQuery)"
    - path: "frontend/src/components/tenders/TenderCard.tsx"
      provides: "Tender detail card per UI-SPEC"
    - path: "frontend/src/components/tenders/WatchlistButton.tsx"
      provides: "Add/remove watchlist toggle with state"
    - path: "frontend/src/components/tenders/DashboardWatchlist.tsx"
      provides: "Dashboard watchlist section with empty state"
  key_links:
    - from: "frontend/src/app/(dashboard)/tenders/page.tsx"
      to: "GET /api/tenders/{number_anno}"
      via: "react-query useQuery enabled on submit"
      pattern: "api.get.*tenders"
    - from: "frontend/src/components/tenders/WatchlistButton.tsx"
      to: "POST /api/watchlist + DELETE /api/watchlist/{number_anno}"
      via: "api.post / api.delete with react-query invalidation"
      pattern: "api\\.(post|delete).*watchlist"
---

<objective>
Wave 3 — the frontend vertical slice. Depends on Wave 2 backend routes.

Delivers (SRCH-01, SRCH-03, SRCH-04) per 03-UI-SPEC.md:
- `api.ts` gets a `delete` method (Pitfall 7)
- Tender TS types
- StatusBadge (green/gray/amber per status)
- TenderCard (full field layout, KZT formatting, lot list)
- WatchlistButton (add/remove with state + optimistic loading)
- /tenders search page (RHF + zod, react-query useQuery, not-found + error states)
- DashboardWatchlist section + wiring into /dashboard

Purpose: Completes the user-facing vertical slice — paste id → see card → add to watchlist → see on dashboard.
Output: api method, types, 4 components, search page, dashboard section + wiring. Ends with a human visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tender-lookup/03-UI-SPEC.md
@.planning/phases/03-tender-lookup/03-RESEARCH.md
@.planning/phases/03-tender-lookup/03-02-SUMMARY.md
@frontend/src/lib/api.ts
@frontend/src/app/(dashboard)/layout.tsx
@frontend/src/app/(dashboard)/dashboard/page.tsx
@frontend/src/components/profile/CompanyProfileForm.tsx

<interfaces>
From api.ts (existing): api.get, api.post, api.put — apiFetch handles 401 refresh, throws Error(detail) on non-ok.
ADD: delete: <T>(path: string) => apiFetch<T>(path, { method: 'DELETE' })

Backend contracts (from 03-02-SUMMARY.md):
- GET  /api/tenders/{number_anno} -> TenderResponse | 404
- POST /api/watchlist {number_anno} -> WatchlistEntryResponse (201)
- DELETE /api/watchlist/{number_anno} -> 204
- GET  /api/watchlist -> WatchlistEntryResponse[]

TenderResponse fields: number_anno, name_ru, name_kz, total_sum, customer_name_ru,
  customer_name_kz, status_id, status_name_ru, start_date, end_date, publish_date, lots_data, cached_at
lots_data items: { lotNumber/lot_number, nameRu/name_ru, descriptionRu/description_ru, amount } — confirm shape from 03-02-SUMMARY.md
WatchlistEntryResponse: { tender: TenderResponse, notification_on, added_at }

react-query QueryClientProvider already provided by (dashboard)/Providers.tsx (Wave 0).
</interfaces>

<ui_contract>
Source of truth for ALL visual decisions: 03-UI-SPEC.md. Key bindings:
- Page h1: "Поиск тендера" — text-2xl font-semibold mb-6
- Input label "Номер объявления", placeholder "Например: 123456"
- CTA "Найти тендер" / loading "Поиск..." — bg-blue-600 text-white rounded px-4 py-2 font-semibold disabled:opacity-50
- TenderCard container: bg-gray-50 border border-gray-200 rounded-lg p-6 mt-6, <article>
- Label style text-sm font-semibold text-gray-500; value text-sm text-gray-900
- totalSum: Intl.NumberFormat('ru-KZ', {style:'currency',currency:'KZT',maximumFractionDigits:0})
- endDate: DD.MM.YYYY, "—" if null
- StatusBadge: open(Принимаются заявки) green bg-green-50 text-green-700; Завершен/Отменен gray; else amber. Shape rounded-full px-2 py-1 text-xs font-semibold
- WatchlistButton default bg-blue-600 text-white "Добавить в список"; active bg-gray-100 text-gray-700 border border-gray-300 "В списке" hover red; loading opacity-50 cursor-not-allowed
- Not-found copy: "Тендер с номером {ID} не найден на портале goszakup.gov.kz. Проверьте номер и попробуйте снова."
- Network error copy: "Не удалось загрузить данные. Проверьте соединение и попробуйте ещё раз."
- Dashboard section h2 "Отслеживаемые тендеры" text-xl font-semibold mb-4 mt-8; empty "Список пуст" / "Найдите тендер и нажмите «Добавить в список»."
- a11y: focus ring focus:ring-2 focus:ring-blue-500; WatchlistButton aria-pressed; spinner role="status" + sr-only "Загрузка..."; card <article>; lots <ul>/<li>
</ui_contract>
</context>

<tasks>

<task id="03-03-01" type="auto">
  <name>Task 1: api.delete, tender types, StatusBadge, TenderCard, WatchlistButton</name>
  <files>
    frontend/src/lib/api.ts,
    frontend/src/types/tender.ts,
    frontend/src/components/tenders/StatusBadge.tsx,
    frontend/src/components/tenders/TenderCard.tsx,
    frontend/src/components/tenders/WatchlistButton.tsx
  </files>
  <read_first>
    - frontend/src/lib/api.ts — add delete next to get/post/put (Pitfall 7)
    - frontend/src/components/profile/CompanyProfileForm.tsx — established 'use client' + Tailwind + api usage style
    - .planning/phases/03-tender-lookup/03-UI-SPEC.md — components 1-3 (TenderCard, WatchlistButton, StatusBadge), Color, Typography, Copywriting
    - .planning/phases/03-tender-lookup/03-02-SUMMARY.md — exact lots_data item key casing returned by backend
  </read_first>
  <action>
    Edit frontend/src/lib/api.ts: add `delete: <T>(path: string) => apiFetch<T>(path, { method: 'DELETE' })` to the exported api object (Pitfall 7). Note DELETE returns 204 with no body — make apiFetch tolerant of empty bodies (if res.status === 204 return undefined as T).

    Create frontend/src/types/tender.ts: export interface Tender (TenderResponse fields from <interfaces>), Lot (lot item), WatchlistEntry ({ tender: Tender; notification_on: boolean; added_at: string }). Match the key casing the backend actually returns (snake_case per Pydantic) — confirm from 03-02-SUMMARY.md.

    Create frontend/src/components/tenders/StatusBadge.tsx ('use client' not required if pure): props { statusName: string | null }. Render <span> with rounded-full px-2 py-1 text-xs font-semibold; color by status per UI-SPEC: "Принимаются заявки" → bg-green-50 text-green-700; "Завершен"/"Отменен" → bg-gray-100 text-gray-600; else bg-yellow-50 text-yellow-700.

    Create frontend/src/components/tenders/TenderCard.tsx: props { tender: Tender; children?: ReactNode } where children is the WatchlistButton slot. Render as <article className="bg-gray-50 border border-gray-200 rounded-lg p-6 mt-6">. Title nameRu (text-xl font-semibold mb-4). Label/value pairs for Заказчик, Номер объявления, Сумма (KZT format), Дедлайн (DD.MM.YYYY or "—"), Статус (<StatusBadge>). Lots section "Лоты ({n})" with <ul>/<li>, each lot flex justify-between, name left, amount KZT right, "Лот {lotNumber}" prefix. Render children (button) at bottom. Use the exact Tailwind classes and copy from UI-SPEC.

    Create frontend/src/components/tenders/WatchlistButton.tsx ('use client'): props { numberAnno: string; isWatching: boolean; onChange?: () => void; compact?: boolean }. Internal loading state. Click: if isWatching call api.delete(`/api/watchlist/${numberAnno}`) else api.post('/api/watchlist', { number_anno: numberAnno }); on success call onChange (parent invalidates react-query). Default style bg-blue-600 text-white "Добавить в список"; active bg-gray-100 text-gray-700 border "В списке" with hover red; loading opacity-50 cursor-not-allowed. aria-pressed={isWatching}, aria-label changes with state, focus ring. compact variant uses text-xs px-2 py-1. On error show inline text-red-500 text-sm mt-1 with the UI-SPEC remove/add error copy.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8 | grep -E "(error|Error|Failed)" && echo "BUILD_FAIL" || echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "delete:" frontend/src/lib/api.ts` returns 1 or more
    - Source: `grep -c "204" frontend/src/lib/api.ts` returns 1 or more (empty-body tolerance)
    - Source: `grep -c "bg-green-50" frontend/src/components/tenders/StatusBadge.tsx` returns 1 or more
    - Source: `grep -c "article" frontend/src/components/tenders/TenderCard.tsx` returns 1 or more
    - Source: `grep -c "ru-KZ" frontend/src/components/tenders/TenderCard.tsx` returns 1 or more
    - Source: `grep -Ec "api\.(post|delete).*watchlist" frontend/src/components/tenders/WatchlistButton.tsx` returns 2 or more
    - Source: `grep -c "aria-pressed" frontend/src/components/tenders/WatchlistButton.tsx` returns 1 or more
    - Behavior: `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    api.delete added with 204 tolerance; types + StatusBadge + TenderCard + WatchlistButton match UI-SPEC; frontend builds.
  </done>
</task>

<task id="03-03-02" type="auto">
  <name>Task 2: /tenders search page + DashboardWatchlist + dashboard wiring</name>
  <files>
    frontend/src/app/(dashboard)/tenders/page.tsx,
    frontend/src/components/tenders/DashboardWatchlist.tsx,
    frontend/src/app/(dashboard)/dashboard/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/dashboard/page.tsx — current 'use client' dashboard with profile link; add a tenders link + watchlist section
    - frontend/src/components/profile/CompanyProfileForm.tsx — RHF + zodResolver + zod pattern to replicate
    - frontend/src/components/tenders/TenderCard.tsx — props contract from Task 1
    - frontend/src/components/tenders/WatchlistButton.tsx — props contract from Task 1
    - .planning/phases/03-tender-lookup/03-UI-SPEC.md — component 1 (search page), component 4 (dashboard watchlist), Interaction Contract, Copywriting
    - .planning/phases/03-tender-lookup/03-RESEARCH.md — Pattern 7 (search page useQuery enabled-on-submit), Pitfall 6 (QueryClientProvider already provided)
  </read_first>
  <action>
    Create frontend/src/app/(dashboard)/tenders/page.tsx as 'use client' per 03-RESEARCH.md Pattern 7:
    - RHF + zodResolver, schema z.object({ number_anno: z.string().min(1,'Введите номер объявления').max(100) })
    - useState queryNumber; useQuery({ queryKey:['tender', queryNumber], queryFn: () => api.get<Tender>(`/api/tenders/${queryNumber}`), enabled: queryNumber !== null, retry: false })
    - onSubmit sets queryNumber = values.number_anno.trim()
    - layout max-w-2xl; h1 "Поиск тендера"; input label + placeholder per UI-SPEC; submit button toggles "Найти тендер"/"Поиск..." disabled while isLoading; spinner role="status" sr-only "Загрузка..."
    - On isError: distinguish 404 (message includes "не найден" or status) → render not-found copy "Тендер с номером {queryNumber} не найден на портале goszakup.gov.kz..."; other errors → network error copy
    - On success: render <TenderCard tender={tender}><WatchlistButton numberAnno=... isWatching={?} onChange={refetch watchlist} /></TenderCard>. Determine isWatching by also querying GET /api/watchlist (useQuery key ['watchlist']) and checking membership; invalidate ['watchlist'] on WatchlistButton onChange via queryClient.invalidateQueries.

    Create frontend/src/components/tenders/DashboardWatchlist.tsx as 'use client':
    - useQuery({ queryKey:['watchlist'], queryFn: () => api.get<WatchlistEntry[]>('/api/watchlist') })
    - h2 "Отслеживаемые тендеры" text-xl font-semibold mb-4 mt-8
    - Empty (length 0): render "Список пуст" heading + "Найдите тендер и нажмите «Добавить в список»." body
    - Populated: bg-gray-50 border rounded-lg divide-y; each row flex items-center justify-between px-4 py-3, left = tender.name_ru (text-sm font-semibold) + number_anno (text-xs text-gray-500 ml-2), right = <StatusBadge> + <WatchlistButton compact isWatching onChange={invalidate ['watchlist']}> (remove variant)

    Edit frontend/src/app/(dashboard)/dashboard/page.tsx: add a <Link href="/tenders">Поиск тендеров</Link> in the nav, and render <DashboardWatchlist /> below the nav.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8 | grep -E "(error|Error|Failed)" && echo "BUILD_FAIL" || echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "'use client'" "frontend/src/app/(dashboard)/tenders/page.tsx"` returns 1
    - Source: `grep -c "useQuery" "frontend/src/app/(dashboard)/tenders/page.tsx"` returns 1 or more
    - Source: `grep -c "TenderCard" "frontend/src/app/(dashboard)/tenders/page.tsx"` returns 1 or more
    - Source: `grep -c "не найден" "frontend/src/app/(dashboard)/tenders/page.tsx"` returns 1 or more
    - Source: `grep -c "Отслеживаемые тендеры" frontend/src/components/tenders/DashboardWatchlist.tsx` returns 1
    - Source: `grep -c "Список пуст" frontend/src/components/tenders/DashboardWatchlist.tsx` returns 1
    - Source: `grep -c "DashboardWatchlist" "frontend/src/app/(dashboard)/dashboard/page.tsx"` returns 1 or more
    - Source: `grep -c "/tenders" "frontend/src/app/(dashboard)/dashboard/page.tsx"` returns 1 or more
    - Behavior: `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Search page does lookup with not-found + error states; dashboard shows watchlist with empty state; reachable from /dashboard; frontend builds.
  </done>
</task>

<task id="03-03-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of the tender-lookup vertical slice</name>
  <what-built>
    A complete vertical slice: /tenders search page, TenderCard, WatchlistButton, and the dashboard
    "Отслеживаемые тендеры" section. Backend routes (Wave 2) and react-query provider (Wave 0) are in place.
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload` (ensure GOSZAKUP_API_TOKEN is set in .env and DB migrated: `alembic upgrade head`)
    2. Start frontend: `cd frontend && npm run dev`
    3. Log in (Phase 2 account), navigate to /tenders
    4. Enter a REAL open tender number (from Wave 0 SPIKE findings) → click "Найти тендер"
    5. Verify TenderCard shows: title, Заказчик, Номер объявления, Сумма (formatted "… ₸"), Дедлайн (DD.MM.YYYY), Статус badge (green if "Принимаются заявки"), and the lots list
    6. Click "Добавить в список" → button switches to "В списке"
    7. Go to /dashboard → confirm the tender appears under "Отслеживаемые тендеры" with its status badge
    8. Enter a clearly-invalid number on /tenders → confirm "Тендер … не найден на портале goszakup.gov.kz" message (no crash)
    9. On the dashboard watchlist row, remove the tender → confirm it disappears and the empty state "Список пуст" shows when none remain
  </how-to-verify>
  <acceptance_criteria>
    - Behavior: TenderCard renders all six fields (title, customer, amount, deadline, status, lots) for a real tender (VALIDATION 3-03-01)
    - Behavior: unknown id shows the not-found message, not a stack trace (VALIDATION 3-03-02)
    - Behavior: add/remove watchlist updates the UI and persists across reload (VALIDATION 3-03-03)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 9 steps pass, or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend API | Authenticated via httpOnly cookie (api.ts credentials:include) |
| rendered tender data → DOM | External goszakup data rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Stored XSS via tender name/lots from goszakup | mitigate | All values rendered as React children (auto-escaped); no dangerouslySetInnerHTML anywhere in tender components |
| T-03-03-02 | Information Disclosure | react-query caching tender data across users on shared device | accept | QueryClient scoped to dashboard route group; logout clears auth; acceptable for single-user desktop-first MVP |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- Manual: visual checkpoint 03-03-03 all 9 steps pass
- Full vertical slice works end-to-end against the live backend
</verification>

<success_criteria>
- SRCH-01: search returns card or clear not-found message
- SRCH-03: card shows title, lots, customer, amount, deadline, status
- SRCH-04: add/remove watchlist works and persists, visible on dashboard
- Frontend builds; human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-tender-lookup/03-03-SUMMARY.md` recording:
- Components created and their props contracts
- How isWatching is determined and watchlist cache invalidation flow
- Visual checkpoint result
- Any UI-SPEC deviations (should be none)
</output>
