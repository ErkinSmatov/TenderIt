---
phase: 05-eds-signing-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/ncalayer.ts
  - frontend/src/hooks/useNCALayer.ts
  - frontend/src/hooks/__tests__/useNCALayer.test.ts
  - frontend/src/components/signing/NCALayerStatus.tsx
  - frontend/src/components/signing/CertificateInfo.tsx
  - frontend/src/components/signing/InstallGuide.tsx
  - .planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md
autonomous: false
requirements: [SIGN-01, SIGN-02, SIGN-03, SIGN-04, SIGN-05]
user_setup: []
must_haves:
  truths:
    - "The UI shows a live NCALayer connectivity indicator (green connected / red disconnected)"
    - "When NCALayer is unreachable the UI shows an install guide link"
    - "Before signing the user sees the certificate owner name and expiry date"
    - "A warning appears when the certificate expires within 30 days"
    - "useNCALayer.signXml dispatches dual-mode: 1.x commonUtils+array+raw-xml, 2.x basics+object+base64"
    - "The exact NCALayer/browser call for goszakup Gamma price encryption (step 7) is documented"
  artifacts:
    - path: "frontend/src/hooks/useNCALayer.ts"
      provides: "Dual-mode NCALayer WebSocket hook (connect, getCertificates, signXml, createCMSSignatureFromBase64)"
      min_lines: 120
    - path: "frontend/src/components/signing/NCALayerStatus.tsx"
      provides: "SIGN-01 status indicator + SIGN-05 install guidance"
    - path: "frontend/src/components/signing/CertificateInfo.tsx"
      provides: "SIGN-02 cert display + SIGN-03 <30-day warning"
    - path: ".planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md"
      provides: "Resolved WS/WebCrypto method for step 7 (unblocks 05-03)"
  key_links:
    - from: "frontend/src/components/signing/NCALayerStatus.tsx"
      to: "frontend/src/hooks/useNCALayer.ts"
      via: "hook status field"
      pattern: "useNCALayer"
    - from: "frontend/src/hooks/useNCALayer.ts"
      to: "wss://127.0.0.1:13579"
      via: "new WebSocket"
      pattern: "127.0.0.1:13579"
---

<objective>
Build the browser-only NCALayer integration: the `useNCALayer()` React hook (dual-mode 1.x/2.x per
SPIKE-02) and the signing UI primitives — connectivity status (SIGN-01), install guidance (SIGN-05),
certificate display (SIGN-02) with a <30-day expiry warning (SIGN-03), and the `signXml` /
`createCMSSignatureFromBase64` methods used by the goszakup auth + Gamma-sign flows (SIGN-04).
Also resolve the CRITICAL open question: the exact call for goszakup Gamma price encryption (step 7).

Purpose: NCALayer runs on the user's machine and the backend NEVER connects to it (CLAUDE.md rule 1).
This plan produces the sole browser bridge to the certificate, plus the reusable status/cert UI that
the wizard (05-03) mounts. It ships and tests independently of the backend.

Output: types, hook, three signing components, a Jest/RTL hook test, and GAMMA-ENCRYPTION-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-eds-signing-submission/05-CONTEXT.md
@.planning/phases/05-eds-signing-submission/05-RESEARCH.md
@CLAUDE.md
@frontend/spikes/SPIKE-02-FINDINGS.md

<interfaces>
<!-- Reuse existing UI + patterns. -->
From frontend/src/components/ui/ — existing primitives (alert, button, etc.). Reuse Alert/Button, do not re-create.
From frontend/src/app/(dashboard)/documents/page.tsx — 'use client' component style, Tailwind class conventions.
NCALayer protocol (SPIKE-02, D-05-07): URL wss://127.0.0.1:13579; version broadcast on connect
`{"result":{"version":"1.4"}}`; isLegacy = parseInt(version) < 2.
signXml 1.x: `{module:"kz.gov.pki.knca.commonUtils", method:"signXml", args:["PKCS12","SIGNATURE",<raw xml>,"",""]}` → resp.responseObject.
signXml 2.x: `{module:"kz.gov.pki.knca.basics", method:"sign", args:{tokenType:"PKCS12",keyType:"SIGNATURE",xmlToSign:<base64>}}` → resp.result.
createCMSSignatureFromBase64 1.x: commonUtils method args ["PKCS12","SIGNATURE",<base64>,false] → resp.responseObject.
Only keyType=SIGNATURE certs shown in UI (exclude AUTH certs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useNCALayer hook (dual-mode) + types</name>
  <files>frontend/src/types/ncalayer.ts, frontend/src/hooks/useNCALayer.ts, frontend/src/hooks/__tests__/useNCALayer.test.ts</files>
  <read_first>
    - frontend/spikes/SPIKE-02-FINDINGS.md (exact WS envelopes, version detection, responseObject vs result)
    - .planning/phases/05-eds-signing-submission/05-RESEARCH.md lines 566-641 (hook reference), 726-738 (base64 SAX pitfall)
    - .planning/phases/05-eds-signing-submission/05-CONTEXT.md lines 198-231 (D-05-07 states + UI states)
    - frontend/src/hooks/ (empty dir — create it) and frontend/src/lib/api.ts (module export style)
    - frontend package.json (confirm test runner: jest/vitest + @testing-library/react availability)
  </read_first>
  <behavior>
    - Given version "1.4", signXml sends commonUtils + array args with the RAW xml string (NOT base64) and resolves to resp.responseObject.
    - Given version "2.1", signXml sends basics + object args with base64(xmlToSign) and resolves to resp.result.
    - status transitions: 'disconnected' → 'connecting' → 'connected' on open+version broadcast; 'signing' during a sign call; 'error' on WS error/close.
    - getCertificates returns only keyType SIGNATURE entries; each Certificate carries subjectCommonName + notAfter (Date).
    - certExpiresWithinDays(cert, 30) returns true when notAfter < now+30d.
  </behavior>
  <action>
    Create frontend/src/types/ncalayer.ts exporting `Certificate` ({serialNumber, subjectCommonName, issuerCommonName,
    notBefore, notAfter}), `NCALayerStatus` union ('disconnected'|'connecting'|'connected'|'signing'|'error'), and
    `NCALayerHookResult` (status, version, certificates, error, connect(), getCertificates(), signXml(xml),
    createCMSSignatureFromBase64(base64)). Create frontend/src/hooks/useNCALayer.ts implementing the hook exactly per
    SPIKE-02: open `new WebSocket("wss://127.0.0.1:13579")`; on open set 'connecting', read the version broadcast to
    set version + 'connected'; compute `isLegacy = parseInt(version ?? "1") < 2`. Implement a `sendRequest(req)`
    promise wrapper (one in-flight request; resolves on next onmessage JSON). Implement signXml dual-mode — 1.x raw
    xml in args[2] (NEVER base64 — SAX pitfall), 2.x base64 xmlToSign — and createCMSSignatureFromBase64 1.x
    commonUtils. Filter certificates to keyType SIGNATURE only. Export a helper `certExpiresWithinDays(cert, days)`.
    Handle onerror/onclose → status 'error' with a human message. All of this is browser-only; add no server calls.
    Create frontend/src/hooks/__tests__/useNCALayer.test.ts with a mock WebSocket asserting the dual-mode branch
    selection and the status transitions in <behavior>.
  </action>
  <verify>
    <automated>cd frontend && npx jest src/hooks/__tests__/useNCALayer.test.ts 2>/dev/null || npx vitest run src/hooks/__tests__/useNCALayer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts that for version "1.4" the WS payload contains `"module":"kz.gov.pki.knca.commonUtils"` and the raw xml (not base64) in args.
    - Test asserts that for version "2.1" the payload uses `kz.gov.pki.knca.basics` and a base64 xmlToSign.
    - `grep -q '127.0.0.1:13579' frontend/src/hooks/useNCALayer.ts`.
    - Hook never imports from '@/lib/api' or performs fetch (`grep -c "fetch(" frontend/src/hooks/useNCALayer.ts` == 0) — browser-only, no server round-trip.
    - certExpiresWithinDays exported and covered by at least one test case.
  </acceptance_criteria>
  <done>Hook + types implemented, dual-mode branch + status transitions covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Signing UI — NCALayerStatus, CertificateInfo, InstallGuide</name>
  <files>frontend/src/components/signing/NCALayerStatus.tsx, frontend/src/components/signing/CertificateInfo.tsx, frontend/src/components/signing/InstallGuide.tsx</files>
  <read_first>
    - frontend/src/app/(dashboard)/documents/page.tsx (client component + Alert usage + Tailwind conventions)
    - frontend/src/components/documents/ (component structure, prop typing conventions)
    - frontend/src/hooks/useNCALayer.ts (from Task 1 — consumed here)
    - frontend/src/components/ui/ (Alert, Button primitives to reuse)
    - .planning/phases/05-eds-signing-submission/05-CONTEXT.md lines 226-231 (UI states mapping)
  </read_first>
  <action>
    Create three 'use client' components, all in Russian UI copy:
    NCALayerStatus.tsx (SIGN-01/SIGN-05): takes the hook result; renders a green dot + "NCALayer подключён (v{version})"
    when status==='connected', a red dot + "NCALayer недоступен" + a "Подключить" button (calls connect()) otherwise;
    when status is 'disconnected' or 'error', render <InstallGuide/>. Expose a `disabled` derivation the parent can use
    to disable the Sign button when not connected.
    CertificateInfo.tsx (SIGN-02/SIGN-03): takes a Certificate; shows subjectCommonName (владелец) and notAfter (действует
    до, formatted ru-RU); if certExpiresWithinDays(cert,30) render a persistent warning Alert "Сертификат истекает через
    N дн. — обновите ЭЦП".
    InstallGuide.tsx (SIGN-05): static guidance with an external link to the NCALayer download page and a one-line "запустите
    NCALayer и обновите страницу" instruction.
    Reuse existing ui/Alert and ui/Button — do not build new primitives.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'useNCALayer' frontend/src/components/signing/NCALayerStatus.tsx` (consumes the hook, no duplicated WS logic).
    - CertificateInfo calls certExpiresWithinDays with 30 (`grep -q 'certExpiresWithinDays' frontend/src/components/signing/CertificateInfo.tsx`).
    - NCALayerStatus renders InstallGuide on disconnected/error (`grep -q 'InstallGuide' frontend/src/components/signing/NCALayerStatus.tsx`).
    - `npx tsc --noEmit` exits 0 (no type errors).
  </acceptance_criteria>
  <done>Three signing components compile, wired to the hook, Russian copy, expiry warning present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Resolve Gamma encryption (step 7) — investigation + human capture</name>
  <files>.planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md</files>
  <action>
    First, automated best-effort: fetch the goszakup priceoffers JS bundle and grep for the WebSocket `.send(` call
    around the encryption step and any `crypto.subtle` (WebCrypto) usage; scaffold GAMMA-ENCRYPTION-FINDINGS.md with a
    "Confirmed step-7 method" section and record whatever the public JS reveals. Then hand off to the human capture
    below to confirm the exact request. Update the doc to state precisely which useNCALayer method 05-03 must call
    (existing signXml/createCMS, a NEW gammaEncrypt method to add to the hook, or a pure browser-WebCrypto path).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md && grep -q "Confirmed step-7 method" .planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md</automated>
  </verify>
  <done>GAMMA-ENCRYPTION-FINDINGS.md records the confirmed step-7 method (or a documented deferral assumption) and names the exact hook method 05-03 must call.</done>
  <acceptance_criteria>
    - GAMMA-ENCRYPTION-FINDINGS.md exists and contains a "Confirmed step-7 method" section.
    - The doc explicitly names which useNCALayer method (or WebCrypto path) 05-03 will invoke for price encryption.
  </acceptance_criteria>
  <what-built>
    A findings doc scaffold at .planning/phases/05-eds-signing-submission/GAMMA-ENCRYPTION-FINDINGS.md plus a
    best-effort automated attempt to fetch and grep the goszakup priceoffers JS for the WS `.send(` call shape and
    any WebCrypto (`crypto.subtle`) usage around the encryption step. The executor records what it can determine
    from the public JS; the definitive request format requires a live authenticated DevTools capture.
  </what-built>
  <how-to-verify>
    1. Log into a real goszakup supplier account, open a tender application, reach the price (Гамма-шифрование) step.
    2. Open DevTools → Network (WS) and Sources; set a breakpoint on `ws.send(` in the priceoffers script (or watch the WS frames panel).
    3. Trigger the price-encryption action and capture: the exact JSON sent to NCALayer for step 7 (module, method, args), OR confirm it is browser `crypto.subtle`/CMS done client-side without NCALayer.
    4. Paste the captured request/response (redact cert/key material) into GAMMA-ENCRYPTION-FINDINGS.md under "Confirmed step-7 method", and state which useNCALayer method 05-03 must call (existing signXml/createCMS, a NEW gammaEncrypt method, or a pure-WebCrypto path).
  </how-to-verify>
  <resume-signal>Type "approved" once GAMMA-ENCRYPTION-FINDINGS.md records the confirmed step-7 method (or "blocked: no goszakup account" to defer with a documented assumption).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → NCALayer (localhost WS) | Signing happens on the user's device; private keys never leave it |
| browser → external NCALayer download link | Static outbound link (SIGN-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Spoofing | wss://127.0.0.1:13579 endpoint | accept | NCALayer is a trusted localhost app; browser-only per CLAUDE.md rule 1; no server trust placed in it |
| T-05-11 | Information Disclosure | certificate rendering | mitigate | Display only subjectCommonName + notAfter; never render private-key or full DN secrets; no cert bytes sent to backend in this plan |
| T-05-12 | Tampering | base64 vs raw xml dispatch | mitigate | Enforce isLegacy branch (1.x raw xml) to avoid SAX-break; covered by unit test |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- Hook test passes (dual-mode + status transitions).
- GAMMA-ENCRYPTION-FINDINGS.md contains a "Confirmed step-7 method" section (or a documented deferral assumption) before 05-03 starts.
</verification>

<success_criteria>
- SIGN-01 live status indicator; SIGN-05 install guide on disconnect.
- SIGN-02 cert owner + expiry; SIGN-03 <30-day persistent warning.
- SIGN-04 signXml + createCMSSignatureFromBase64 dual-mode implemented and tested.
- Step-7 Gamma method resolved and documented (unblocks 05-03).
</success_criteria>

<output>
After completion, create `.planning/phases/05-eds-signing-submission/05-02-SUMMARY.md`
</output>
