---
phase: 05-eds-signing-submission
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified:
  - frontend/src/app/(dashboard)/applications/page.tsx
  - frontend/src/app/(dashboard)/applications/[id]/page.tsx
  - frontend/src/components/applications/ApplicationCard.tsx
  - frontend/src/components/applications/ApplicationStatusBadge.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: false
requirements: [APPL-01, APPL-02, APPL-05, APPL-06]
must_haves:
  truths:
    - "User can view a list of all their applications with current status (APPL-05)"
    - "User can open an application detail page showing the state-machine history"
    - "The detail page polls status every 30s so open→submitting→submitted updates live (APPL-04)"
    - "An errored application shows the raw portal error message (APPL-06)"
    - "A Sidebar nav link points to /applications"
  artifacts:
    - path: "frontend/src/app/(dashboard)/applications/page.tsx"
      provides: "APPL-05 applications history list"
      min_lines: 30
    - path: "frontend/src/app/(dashboard)/applications/[id]/page.tsx"
      provides: "APPL-04 status detail with 30s polling + APPL-06 error surface"
    - path: "frontend/src/components/applications/ApplicationStatusBadge.tsx"
      provides: "Russian status label + color per state-machine state"
  key_links:
    - from: "frontend/src/app/(dashboard)/applications/page.tsx"
      to: "/api/applications"
      via: "react-query api.get"
      pattern: "/api/applications"
    - from: "frontend/src/app/(dashboard)/applications/[id]/page.tsx"
      to: "/api/applications/{id}"
      via: "polling useQuery refetchInterval 30000"
      pattern: "refetchInterval"
---

<objective>
Deliver the application status UI: an applications history list (APPL-05), a detail page that polls status
every 30s to reflect draft → signed → waiting → submitting → submitted transitions (APPL-04), a status
badge with Russian labels, an error state showing the raw portal message (APPL-06), and a Sidebar nav
link. This closes the phase's user-visible loop after 05-03 (create/sign) and 05-04 (auto-submit).

Purpose: Users need to see what they prepared and track the auto-submission outcome. This is the read side
of the applications feature — it consumes the 05-01 endpoints and completes the phase goal.

Output: list page, detail page (polling), card + status-badge components, Sidebar nav entry, human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-eds-signing-submission/05-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- From 05-01 (backend endpoints) + 05-03 (frontend types). -->
GET /api/applications → ApplicationResponse[] (id, tender_id, status, lots_data, document_ids, goszakup_application_id, error_message, retry_count, created_at, ready_at, submitted_at).
GET /api/applications/{id} → ApplicationResponse (404 if not owned).
frontend/src/types/application.ts: ApplicationStatus, ApplicationResponse (created in 05-03).
frontend/src/lib/api.ts: api.get. State machine (D-05-04): draft | signed | waiting | submitting | submitted | error.
frontend/src/app/(dashboard)/documents/page.tsx: react-query useQuery page pattern to mirror.
frontend/src/components/layout/Sidebar.tsx: existing nav — add an "Заявки" link to /applications alongside Documents/Tenders.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status badge + application card</name>
  <files>frontend/src/components/applications/ApplicationStatusBadge.tsx, frontend/src/components/applications/ApplicationCard.tsx</files>
  <read_first>
    - frontend/src/types/application.ts (ApplicationStatus, ApplicationResponse from 05-03)
    - frontend/src/components/documents/ (card component conventions, badge styling)
    - frontend/src/components/ui/ (existing badge/alert primitives to reuse)
    - .planning/phases/05-eds-signing-submission/05-CONTEXT.md lines 132-149 (state machine + Russian labels)
  </read_first>
  <action>
    Create frontend/src/components/applications/ApplicationStatusBadge.tsx mapping each ApplicationStatus to a Russian
    label + Tailwind color: draft→"Черновик" (gray), signed→"Подписано" (blue), waiting→"Ожидает открытия" (amber),
    submitting→"Отправляется" (indigo, animated), submitted→"Отправлено" (green), error→"Ошибка" (red). Create
    frontend/src/components/applications/ApplicationCard.tsx: takes an ApplicationResponse, renders tender_id (linked to
    /applications/{id}), the status badge, created_at (ru-RU), and — when status==='error' — the error_message in a
    destructive Alert (APPL-06). Reuse existing ui primitives; do not create new base components.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Badge maps all six statuses (`grep -c '"' frontend/src/components/applications/ApplicationStatusBadge.tsx` covers draft/signed/waiting/submitting/submitted/error — each label present via `grep -q`).
    - ApplicationCard renders error_message when status is error (`grep -q 'error_message' frontend/src/components/applications/ApplicationCard.tsx`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Status badge (6 states, Russian) + application card with error surface compile.</done>
</task>

<task type="auto">
  <name>Task 2: Applications list + detail (30s polling) + Sidebar nav</name>
  <files>frontend/src/app/(dashboard)/applications/page.tsx, frontend/src/app/(dashboard)/applications/[id]/page.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - frontend/src/app/(dashboard)/documents/page.tsx (useQuery page pattern, Alert on error, loading guard)
    - frontend/src/app/(dashboard)/tenders/page.tsx (dynamic data + linking pattern)
    - frontend/src/components/layout/Sidebar.tsx (nav item structure to extend)
    - frontend/src/components/applications/ (Card + Badge from Task 1)
    - frontend/src/lib/api.ts (api.get)
  </read_first>
  <action>
    Create frontend/src/app/(dashboard)/applications/page.tsx ('use client'): useQuery(['applications']) → GET
    /api/applications, render a list of ApplicationCard newest-first, an empty state ("Пока нет заявок") and an error
    Alert. Create frontend/src/app/(dashboard)/applications/[id]/page.tsx ('use client'): read the id param,
    useQuery(['application', id]) → GET /api/applications/{id} with `refetchInterval: 30000` (APPL-04 live status), show
    the status badge, lots_data (lot + price), attached document_ids count, timestamps (created_at, ready_at,
    submitted_at) as a readable state-history timeline, and — on status==='error' — the raw error_message plus a note
    that the signed submission is retained for manual retry (APPL-06). Add an "Заявки" nav link to /applications in
    frontend/src/components/layout/Sidebar.tsx next to the existing Documents/Tenders links. All copy in Russian.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - List page calls GET /api/applications (`grep -q "/api/applications'" frontend/src/app/(dashboard)/applications/page.tsx` or equivalent).
    - Detail page uses `refetchInterval: 30000` (`grep -q 'refetchInterval' 'frontend/src/app/(dashboard)/applications/[id]/page.tsx'`).
    - Detail page renders error_message on error status (`grep -q 'error_message' 'frontend/src/app/(dashboard)/applications/[id]/page.tsx'`).
    - Sidebar has a /applications link (`grep -q '/applications' frontend/src/components/layout/Sidebar.tsx`).
    - `npx next build` succeeds.
  </acceptance_criteria>
  <done>List + polling detail pages build; Sidebar nav added; error state visible; timeline shown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify application status UI end-to-end</name>
  <files>(none — verification checkpoint only)</files>
  <action>
    No code changes. This is a human verification gate for the UI built in Tasks 1-2. The executor ensures the
    frontend builds and is runnable, then pauses for the human to verify the list, polling detail, error surface,
    and Sidebar nav per the steps below.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Human confirms the applications list, 30s-polling detail, error message surface, and Sidebar link all work; issues (if any) are recorded for fix.</done>
  <acceptance_criteria>
    - Human types "approved" after confirming list + detail polling + error surface + nav.
    - Any reported layout/status/polling defects are captured as follow-up items.
  </acceptance_criteria>
  <what-built>
    The applications history list at /applications, a detail page at /applications/{id} that polls every 30s, a
    six-state Russian status badge, and an error view surfacing the raw portal message. A Sidebar "Заявки" link.
  </what-built>
  <how-to-verify>
    1. Run the frontend (`cd frontend && npm run dev`) and backend; log in.
    2. Visit /applications — confirm the list renders your applications (or the empty state) with correct status badges.
    3. Open one application — confirm the detail shows lots + prices, document count, a readable timestamp timeline, and the correct status badge.
    4. For an application in "error" status (or a seeded one), confirm the raw error_message is shown with the retention note.
    5. Leave the detail page open ~30s and confirm it refetches (status stays current without a manual reload).
    6. Confirm the Sidebar "Заявки" link navigates to /applications.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/status/polling issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/applications[/{id}] | Read of user-owned application state (auth-gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-40 | Information Disclosure | GET /api/applications/{id} | mitigate | Backend (05-01) filters by user_id → 404 for others; UI never requests cross-user ids |
| T-05-41 | Information Disclosure | error_message rendering | accept | Portal error text (e.g. tax-debt notice) shown only to the owning user; no secrets in the message |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npx next build` succeeds.
- Human-verify checkpoint approved (list + polling detail + error surface + nav).
</verification>

<success_criteria>
- APPL-05 applications history list renders with per-state badges.
- APPL-04 detail page polls every 30s and reflects live status transitions.
- APPL-06 error status surfaces the raw portal message + retention note.
- Sidebar "Заявки" nav link present.
</success_criteria>

<output>
After completion, create `.planning/phases/05-eds-signing-submission/05-05-SUMMARY.md`
</output>
