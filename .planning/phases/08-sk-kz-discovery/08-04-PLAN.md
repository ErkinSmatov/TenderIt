---
phase: 08-sk-kz-discovery
plan: 04
type: execute
wave: 2
depends_on:
  - "08-02"
files_modified:
  - frontend/src/types/discovery.ts
  - frontend/src/components/discovery/TenderMatchCard.tsx
autonomous: true
requirements:
  - SC-08-03
  - SC-08-05

must_haves:
  truths:
    - "TenderMatchResponse TypeScript interface has source: string | null and portal_url: string | null fields"
    - "TenderMatchCard renders SourceBadge with blue style for sk_kz and gray style for goszakup"
    - "SourceBadge displays 'SK.KZ' for source==='sk_kz' and 'ГОСЗАКУП' for any other value"
    - "When source==='sk_kz' and portal_url is set, the source badge is wrapped in an anchor tag pointing to portal_url"
    - "When source==='sk_kz' and match is 'participating', a manual submission note is displayed"
    - "Existing TenderMatchCard tests pass — source/portal_url are Optional with sensible defaults"
  artifacts:
    - path: frontend/src/types/discovery.ts
      provides: "TenderMatchResponse with source and portal_url fields"
    - path: frontend/src/components/discovery/TenderMatchCard.tsx
      provides: "SourceBadge component, dynamic source rendering, portal link, sk.kz manual note"
  key_links:
    - from: frontend/src/components/discovery/TenderMatchCard.tsx
      to: frontend/src/types/discovery.ts
      via: "import { TenderMatchResponse } from '@/types/discovery'"
      pattern: "match.source, match.portal_url"
---

<objective>
Replace the hardcoded "goszakup" source badge in TenderMatchCard with a dynamic SourceBadge component, and show a manual submission note for sk.kz tenders when the user has clicked "Участвуем".

Purpose: Without this, sk.kz tenders appear identically to goszakup tenders in the /discovery feed. The source badge + portal link fulfills success criteria #3 and #5 from the frontend perspective.

Output: Updated discovery.ts TypeScript types; updated TenderMatchCard.tsx with SourceBadge, portal link, and sk.kz participation note.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sk-kz-discovery/08-PATTERNS.md
@.planning/phases/08-sk-kz-discovery/08-02-SUMMARY.md

<interfaces>
From frontend/src/types/discovery.ts (read fully — current TenderMatchResponse ends at region):
  Current last field: region: string | null
  Add after region:
    source: string | null      // 'goszakup' | 'sk_kz' — null for legacy records
    portal_url: string | null  // direct link to tender on source portal; null for goszakup

From frontend/src/components/discovery/TenderMatchCard.tsx (read fully before editing):
  Currently hardcoded "Источник" cell (lines ~119-128):
    <span className="inline-flex items-center rounded-full border border-gray-200 bg-gray-100 px-2 py-0.5 text-[10px] font-medium text-gray-600">
      goszakup
    </span>
  Replace the hardcoded span with <SourceBadge source={match.source} portalUrl={match.portal_url} />

  SourceBadge props interface: { source: string | undefined | null; portalUrl?: string | null }
  Color logic:
    sk_kz  → "border-blue-200 bg-blue-50 text-blue-700"
    other  → "border-gray-200 bg-gray-100 text-gray-600"
  Label logic:
    sk_kz  → "SK.KZ"
    other  → "ГОСЗАКУП"

  Manual submission note: when match.source === 'sk_kz' && match.status === 'participating'
    Show a short note below the action buttons (or below the card body):
    "Заявку нужно подать вручную на zakup.sk.kz"
    Style: text-xs text-amber-600 (same pattern as warning text in the app)

  portal_url link: wrap SourceBadge's inner span in <a href={portalUrl} target="_blank" rel="noopener noreferrer">
    Only when portalUrl is truthy.

From 08-PATTERNS.md (section "TenderMatchCard.tsx"):
  SourceBadge component pattern (lines 433-447) — exact className values confirmed
  Existing "Источник" cell location (lines 119-128) — replace only the <span>, not the outer <div>

TypeScript note: match.source is string | null (from the updated TenderMatchResponse). SourceBadge accepts
source: string | undefined | null — handle all three cases with a single conditional.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend discovery.ts — add source and portal_url to TenderMatchResponse</name>
  <files>frontend/src/types/discovery.ts</files>
  <read_first>
    frontend/src/types/discovery.ts (read fully — current structure, JSDoc comments style, field order)
  </read_first>
  <action>
Locate the TenderMatchResponse interface in frontend/src/types/discovery.ts.
Find the last field: "region: string | null".
Add TWO fields immediately after it:

  source: string | null      // 'goszakup' | 'sk_kz' — null for records created before Phase 8
  portal_url: string | null  // direct link to tender on source portal; null for goszakup

Update the JSDoc comment above TenderMatchResponse to mention:
  "Contains denormalized tender fields from a JOIN in the discovery router."
  Add: "source and portal_url are populated from the Tender JOIN (Phase 8)."

Do NOT modify TenderMatchListResponse, ClientFilterResponse, or TenderMatchStatus type.

The TypeScript compiler must accept match.source === 'sk_kz' comparisons (string | null is fine).
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TenderMatchResponse interface has "source: string | null" field (grep confirms)
    - TenderMatchResponse interface has "portal_url: string | null" field (grep confirms)
    - npx tsc --noEmit exits with 0 (no new TypeScript errors introduced)
    - TenderMatchListResponse is unchanged
    - TenderMatchStatus union type is unchanged
  </acceptance_criteria>
  <done>
    frontend/src/types/discovery.ts has source and portal_url on TenderMatchResponse. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TenderMatchCard.tsx — SourceBadge, portal link, sk.kz participation note</name>
  <files>frontend/src/components/discovery/TenderMatchCard.tsx</files>
  <read_first>
    frontend/src/components/discovery/TenderMatchCard.tsx (read fully — locate "Источник" cell at lines ~119-128, understand component props, find action buttons section for placement of manual note),
    .planning/phases/08-sk-kz-discovery/08-PATTERNS.md (section "TenderMatchCard.tsx" — SourceBadge code pattern, lines 433-457)
  </read_first>
  <action>
Make THREE targeted changes to frontend/src/components/discovery/TenderMatchCard.tsx:

CHANGE 1 — Add SourceBadge component function ABOVE the main component (before "export default" or "export function TenderMatchCard"):

function SourceBadge({
  source,
  portalUrl,
}: {
  source: string | undefined | null
  portalUrl?: string | null
}) {
  const label = source === 'sk_kz' ? 'SK.KZ' : 'ГОСЗАКУП'
  const colorClass =
    source === 'sk_kz'
      ? 'border-blue-200 bg-blue-50 text-blue-700'
      : 'border-gray-200 bg-gray-100 text-gray-600'
  const badge = (
    <span
      className={`inline-flex items-center rounded-full border px-2 py-0.5 text-[10px] font-medium ${colorClass}`}
    >
      {label}
    </span>
  )
  if (portalUrl) {
    return (
      <a href={portalUrl} target="_blank" rel="noopener noreferrer">
        {badge}
      </a>
    )
  }
  return badge
}

CHANGE 2 — Replace the hardcoded source badge in the "Источник" cell.
Locate the existing hardcoded span (lines ~119-128):
  <span className="inline-flex items-center rounded-full border border-gray-200 bg-gray-100 px-2 py-0.5 text-[10px] font-medium text-gray-600">
    goszakup
  </span>
Replace with:
  <SourceBadge source={match.source} portalUrl={match.portal_url} />

The outer <div> structure around "Источник" stays unchanged. Only the hardcoded <span> is replaced.

CHANGE 3 — Add manual submission note for sk.kz participating tenders.
Find where action buttons are rendered (the section with "Участвуем" / "Пропустить" buttons or the status section).
After the button row (or after the status badge area for a participating match), add:

{match.source === 'sk_kz' && match.status === 'participating' && (
  <p className="mt-2 text-xs text-amber-600">
    Заявку нужно подать вручную на zakup.sk.kz
  </p>
)}

IMPORTANT: If there is a conditional rendering block where buttons are shown only for certain statuses
(e.g., only for matched/notified), place the sk.kz note INSIDE the "participating" status section
or in the card footer that is always visible. The note must be visible when status === 'participating'
regardless of which tab/filter the user is on.

TypeScript: match.source is string | null (from updated TenderMatchResponse). The === 'sk_kz' check
narrows it safely. No type assertion needed.

Do NOT add any new imports beyond what is already in the file (SourceBadge is defined in the same file).
Do NOT modify any other cell in the details grid.
Do NOT change the action button handlers (participate/skip API calls).
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/frontend && npx tsc --noEmit 2>&1 | head -20 && grep -c "SourceBadge" src/components/discovery/TenderMatchCard.tsx && grep -c "portal_url" src/components/discovery/TenderMatchCard.tsx && grep -c "zakup.sk.kz" src/components/discovery/TenderMatchCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with 0 (no TypeScript errors)
    - "SourceBadge" appears in TenderMatchCard.tsx at least twice (function definition + usage)
    - "goszakup" as a hardcoded string no longer appears inside the Источник cell (grep -n "goszakup" shows only if it appears elsewhere, not as a literal label)
    - "portal_url" appears in TenderMatchCard.tsx (used in SourceBadge props)
    - "zakup.sk.kz" appears in TenderMatchCard.tsx (in the manual submission note)
    - "ГОСЗАКУП" appears in SourceBadge function body (label for goszakup source)
    - Manual note is conditionally rendered: source === 'sk_kz' && status === 'participating'
    - Existing button click handlers are unchanged (participate/skip logic not touched)
  </acceptance_criteria>
  <done>
    TenderMatchCard.tsx has SourceBadge component. Hardcoded "goszakup" badge replaced with dynamic SourceBadge. sk.kz badge shows blue, goszakup shows gray. Portal link wraps badge when portal_url is set. Manual submission note shows for sk.kz participating matches. TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response portal_url → anchor href | Server-provided URL rendered as href; must not allow javascript: or data: URIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | portal_url → anchor href (open redirect / XSS) | mitigate | rel="noopener noreferrer" + target="_blank" prevents opener access; portal_url is computed server-side from a fixed template f"https://zakup.sk.kz/eprocsearch/tender/{number_anno}" — no user-controlled input; number_anno is a numeric string from sk.kz API |
| T-08-11 | Information Disclosure | source field leaks platform identity | accept | source values ('goszakup', 'sk_kz') are not sensitive; tender data is already publicly visible on both portals |
</threat_model>

<verification>
1. `npx tsc --noEmit` in frontend/ — exits 0
2. `grep -c "SourceBadge" frontend/src/components/discovery/TenderMatchCard.tsx` — at least 2
3. `grep -c "SK.KZ" frontend/src/components/discovery/TenderMatchCard.tsx` — at least 1 (in SourceBadge)
4. `grep -c "ГОСЗАКУП" frontend/src/components/discovery/TenderMatchCard.tsx` — at least 1 (in SourceBadge)
5. `grep -n "rel=\"noopener noreferrer\"" frontend/src/components/discovery/TenderMatchCard.tsx` — confirms safe anchor
6. `grep -c "source: string | null" frontend/src/types/discovery.ts` — 1 (in TenderMatchResponse)
</verification>

<success_criteria>
- TenderMatchResponse TypeScript interface has source: string | null and portal_url: string | null
- SourceBadge shows "SK.KZ" (blue) for sk_kz, "ГОСЗАКУП" (gray) for all others
- Portal link wraps badge when portal_url is provided (rel="noopener noreferrer")
- Manual note "Заявку нужно подать вручную на zakup.sk.kz" shown when source=sk_kz and status=participating
- TypeScript compiles without new errors
- No change to participate/skip button handlers
</success_criteria>

<output>
After completion, create .planning/phases/08-sk-kz-discovery/08-04-SUMMARY.md
</output>
