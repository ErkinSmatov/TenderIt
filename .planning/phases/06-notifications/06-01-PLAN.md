---
phase: 06-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0008_add_telegram_link_token.py
  - backend/app/models/user.py
  - backend/app/config.py
  - backend/app/routers/notifications.py
  - backend/app/main.py
  - backend/app/routers/telegram_webhook.py
  - backend/tests/test_notifications.py
autonomous: true
requirements:
  - NOTIF-04

must_haves:
  truths:
    - "POST /api/notifications/telegram/link-token (JWT) returns {deep_link: 'https://t.me/{BOTNAME}?start={43-char-token}'}"
    - "GET /api/notifications/status (JWT) returns {telegram_connected: bool, telegram_chat_id: int|null}"
    - "DELETE /api/notifications/telegram (JWT) → 204, clears telegram_chat_id and telegram_link_token on current user"
    - "Telegram webhook: /start {VALID_TOKEN} sets user.telegram_chat_id = message.from_user.id and clears link token fields"
    - "Telegram webhook: /start {EXPIRED_TOKEN} replies 'Ссылка устарела', does NOT set telegram_chat_id"
    - "All 6 tests in test_notifications.py pass; full backend suite green"
  artifacts:
    - path: "backend/alembic/versions/0008_add_telegram_link_token.py"
      provides: "Migration: op.add_column telegram_link_token (String 64, unique) + telegram_link_token_expires_at (TIMESTAMP timezone)"
      contains: "revision = \"0008\""
    - path: "backend/app/models/user.py"
      provides: "User ORM model extended with telegram_link_token and telegram_link_token_expires_at Mapped fields"
      contains: "telegram_link_token"
    - path: "backend/app/routers/notifications.py"
      provides: "3 JWT-gated endpoints: POST /notifications/telegram/link-token, GET /notifications/status, DELETE /notifications/telegram"
      exports: ["router"]
    - path: "backend/app/routers/telegram_webhook.py"
      provides: "Extended with _handle_start_command helper + message branch before existing early return"
      contains: "_handle_start_command"
    - path: "backend/tests/test_notifications.py"
      provides: "6 async test functions covering all NOTIF-04 behaviors (link-token, status, disconnect, start-handler valid, start-handler expired)"
  key_links:
    - from: "backend/app/routers/notifications.py"
      to: "backend/app/models/user.py"
      via: "current_user.telegram_link_token and telegram_link_token_expires_at written via SQLAlchemy ORM + await db.commit()"
      pattern: "current_user\\.telegram_link_token"
    - from: "backend/app/routers/telegram_webhook.py"
      to: "backend/app/models/user.py"
      via: "_handle_start_command: user.telegram_chat_id = chat_id after successful token validation"
      pattern: "user\\.telegram_chat_id = chat_id"
    - from: "backend/app/main.py"
      to: "backend/app/routers/notifications.py"
      via: "application.include_router(notifications.router, prefix='/api', tags=['notifications'])"
      pattern: "include_router.*notifications"
---

## Phase Goal

**As a** tender manager, **I want to** connect my Telegram account to TenderIt via a single /start command, **so that** I receive automatic alerts the moment a watched tender opens for applications.

<objective>
Deliver the complete backend for Telegram account linking: DB migration, 3 REST endpoints, and the /start TOKEN webhook handler.

Purpose: Phase 7 ARQ workers already guard on `user.telegram_chat_id is None` — this plan sets that field via the deep-link flow (NOTIF-04), activating the full notification pipeline.

Output: migration 0008, notifications.py router, extended telegram_webhook.py, and test_notifications.py with 6 tests all passing.
</objective>

<execution_context>
@/Users/smatov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smatov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/smatov/GitLab/TenderIt/.planning/PROJECT.md
@/Users/smatov/GitLab/TenderIt/.planning/ROADMAP.md
@/Users/smatov/GitLab/TenderIt/.planning/phases/06-notifications/06-CONTEXT.md
@/Users/smatov/GitLab/TenderIt/.planning/phases/06-notifications/06-RESEARCH.md
@/Users/smatov/GitLab/TenderIt/.planning/phases/06-notifications/06-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Do not re-read these files. -->

From backend/app/models/user.py (current state — extend after telegram_chat_id):
  class User(Base):
      __tablename__ = "users"
      id: Mapped[int] = mapped_column(primary_key=True)
      email: Mapped[str] = mapped_column(String(255), unique=True, nullable=False, index=True)
      hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
      is_active: Mapped[bool] = mapped_column(default=True)
      is_verified: Mapped[bool] = mapped_column(default=False)
      created_at: Mapped[datetime] = mapped_column(server_default=func.now())
      # Phase 5 (D-05-06):
      telegram_chat_id: Mapped[Optional[int]] = mapped_column(BigInteger, nullable=True)
      # ADD HERE: telegram_link_token + telegram_link_token_expires_at
  Current imports: from __future__ import annotations, datetime, Optional,
      BigInteger, String, func, Mapped, mapped_column, relationship, Base
  Need to add: import sqlalchemy as sa   (for sa.TIMESTAMP)

From backend/app/config.py (current settings block — append after telegram_webhook_secret):
  telegram_bot_token: str = ""
  webhook_base_url: str = "http://localhost:8000"
  telegram_webhook_secret: str = ""
  # ADD: telegram_bot_username: str = ""

From backend/app/routers/telegram_webhook.py (insertion point at lines 128–130):
  update = Update.de_json(body, bot=None)   # line 126
  if not update.callback_query:
      # Non-callback update — accept silently
      return {"ok": True}                    # INSERT message branch before this return
  existing imports already present: telegram, Update, select, AsyncSession,
      app.config.settings, app.db.get_db, app.models.user.User

From backend/app/main.py (router registration lines 13–84):
  from app.routers import discovery            # line 16 — add notifications on next line
  application.include_router(discovery.router, prefix="/api", tags=["discovery"])  # line 84
  # ADD: application.include_router(notifications.router, prefix="/api", tags=["notifications"])

From backend/app/deps.py:
  async def get_current_user(
      access_token: str | None = Cookie(default=None),
      db: AsyncSession = Depends(get_db),
  ) -> User:   # raises 401 automatically if absent/invalid JWT

From backend/app/routers/tenders.py (DELETE 204 pattern):
  @router.delete("/watchlist/{number_anno}", status_code=204)
  async def remove_watchlist_entry(...) -> Response:
      ...
      return Response(status_code=204)

From backend/alembic/versions/0007_create_tender_matches.py:
  revision: str = "0007"  # → this is down_revision for 0008

Test auth pattern (from backend/tests/test_company_profile.py):
  @pytest_asyncio.fixture(scope="module")
  async def authed_client():
      async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
          email = f"notiftest_{uuid.uuid4().hex[:8]}@example.com"
          with patch("app.routers.auth.store_refresh_token", new=AsyncMock()):
              resp = await ac.post("/api/auth/register", json={"email": email, "password": "SecurePass123"})
          assert resp.status_code == 201
          yield ac

Webhook test pattern (from backend/tests/test_telegram_webhook.py):
  WEBHOOK_SECRET = "test-webhook-secret-xyz"
  app.state.arq_redis = mock_arq_redis
  headers={"X-Telegram-Bot-Api-Secret-Token": WEBHOOK_SECRET}
  monkeypatch.setattr(settings, "telegram_webhook_secret", WEBHOOK_SECRET)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration 0008 — add telegram link-token fields to users + extend User model + config</name>
  <files>
    backend/alembic/versions/0008_add_telegram_link_token.py,
    backend/app/models/user.py,
    backend/app/config.py
  </files>
  <read_first>
    - backend/app/models/user.py — read current fields; extend after telegram_chat_id line
    - backend/app/config.py — read current Settings class; append new field after telegram_webhook_secret
    - backend/alembic/versions/0007_create_tender_matches.py — confirm down_revision = "0007"
    - backend/alembic/versions/0004_create_applications.py — op.add_column on "users" pattern and downgrade pattern
    - .planning/phases/06-notifications/06-PATTERNS.md — Pattern assignments for migration 0008 and user.py
  </read_first>
  <action>
    Create backend/alembic/versions/0008_add_telegram_link_token.py with:
    - Docstring: "add_telegram_link_token"; revision = "0008"; down_revision = "0007"; branch_labels = None; depends_on = None
    - imports: from typing import Sequence, Union; import sqlalchemy as sa; from alembic import op
    - upgrade(): call op.add_column("users", sa.Column("telegram_link_token", sa.String(64), nullable=True)) first; then op.create_index("idx_users_telegram_link_token", "users", ["telegram_link_token"], unique=True); then op.add_column("users", sa.Column("telegram_link_token_expires_at", sa.TIMESTAMP(timezone=True), nullable=True))
    - downgrade(): reverse order — op.drop_column("users", "telegram_link_token_expires_at"); op.drop_index("idx_users_telegram_link_token", table_name="users"); op.drop_column("users", "telegram_link_token")

    Extend backend/app/models/user.py:
    - Add import: import sqlalchemy as sa  (on its own line after existing imports)
    - After the telegram_chat_id field, add two new mapped fields with a Phase 6 comment:
      telegram_link_token as Mapped[Optional[str]] = mapped_column(String(64), nullable=True, unique=True, index=True)
      telegram_link_token_expires_at as Mapped[Optional[datetime]] = mapped_column(sa.TIMESTAMP(timezone=True), nullable=True)

    Extend backend/app/config.py:
    - Append after telegram_webhook_secret: telegram_bot_username: str = ""
    - Add a comment on the line above: "# Phase 6: Telegram bot username for deep-link generation (NOTIF-04, D-09)"
    - Empty default is intentional — deep_link construction degrades gracefully in dev (broken URL) but non-fatal

    Do NOT create or run alembic upgrade in this task — the executor will run it during verify.
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && alembic upgrade head && alembic downgrade -1 && alembic upgrade head</automated>
    <automated>cd /Users/smatov/GitLab/TenderIt && grep -c "telegram_link_token" backend/app/models/user.py</automated>
    <automated>cd /Users/smatov/GitLab/TenderIt && grep -c "telegram_bot_username" backend/app/config.py</automated>
  </verify>
  <acceptance_criteria>
    - alembic upgrade head exits 0 with migration 0008 applied; alembic downgrade -1 exits 0; second upgrade exits 0
    - grep for "telegram_link_token" in user.py returns count >= 2 (field + expires field use the name)
    - grep for "telegram_bot_username" in config.py returns count >= 1
    - 0008 migration file contains: revision = "0008", down_revision = "0007", "idx_users_telegram_link_token", unique=True, sa.TIMESTAMP(timezone=True)
    - User model telegram_link_token field has unique=True and index=True attributes
    - User model telegram_link_token_expires_at field uses sa.TIMESTAMP(timezone=True) — NOT DateTime without timezone
  </acceptance_criteria>
  <done>Migration 0008 applies and rolls back cleanly; User ORM model has both link-token fields; config has telegram_bot_username.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notifications router (3 endpoints) + main.py registration + endpoint tests</name>
  <files>
    backend/app/routers/notifications.py,
    backend/app/main.py,
    backend/tests/test_notifications.py
  </files>
  <read_first>
    - backend/app/routers/tenders.py — JWT auth dep pattern, DELETE 204 → Response(status_code=204) pattern, router = APIRouter(), from app.deps import get_current_user
    - backend/app/main.py — read current import block (lines 13–16) and include_router block (lines 76–84); extend both
    - backend/tests/test_company_profile.py — authed_client fixture pattern with patch("app.routers.auth.store_refresh_token", new=AsyncMock()) and module-scoped fixture
    - backend/tests/conftest.py — db_session fixture, session/module scope details
    - .planning/phases/06-notifications/06-PATTERNS.md — Pattern 4 (notifications.py), main.py extension, Shared Patterns section
    - .planning/phases/06-notifications/06-RESEARCH.md — Pattern 3 (token generation), Pitfall 2 (timezone-aware datetime), Pitfall 6 (missing telegram_bot_username)
  </read_first>
  <behavior>
    - POST /api/notifications/telegram/link-token with valid JWT and settings.telegram_bot_username = "testbot":
      returns 200 JSON with key "deep_link" whose value starts with "https://t.me/testbot?start=" and token portion is 43 chars
    - POST /api/notifications/telegram/link-token without JWT cookie: returns 401
    - GET /api/notifications/status for a user with telegram_chat_id = None: returns {telegram_connected: false, telegram_chat_id: null}
    - GET /api/notifications/status for a user with telegram_chat_id = 12345 (set via DB): returns {telegram_connected: true, telegram_chat_id: 12345}
    - DELETE /api/notifications/telegram with valid JWT: returns 204; subsequent GET /notifications/status returns telegram_connected: false
  </behavior>
  <action>
    Create backend/app/routers/notifications.py:
    - Module docstring explaining the 3 endpoints and security: all require get_current_user JWT dep; telegram_link_token generated with secrets.token_urlsafe(32); IDOR-safe (no user_id in request body)
    - Imports: from __future__ import annotations; import secrets; from datetime import datetime, timezone, timedelta; from fastapi import APIRouter, Depends, Response; from sqlalchemy.ext.asyncio import AsyncSession; from app.config import settings; from app.db import get_db; from app.deps import get_current_user; from app.models.user import User
    - router = APIRouter()
    - POST /notifications/telegram/link-token: deps = get_current_user + get_db; generate token via secrets.token_urlsafe(32); set current_user.telegram_link_token = token; set current_user.telegram_link_token_expires_at = datetime.now(timezone.utc) + timedelta(minutes=15); await db.commit(); build deep_link = f"https://t.me/{settings.telegram_bot_username}?start={token}"; return {"deep_link": deep_link}
    - GET /notifications/status: dep = get_current_user only (no db needed — user loaded by dep); return {"telegram_connected": current_user.telegram_chat_id is not None, "telegram_chat_id": current_user.telegram_chat_id}
    - DELETE /notifications/telegram, status_code=204: deps = get_current_user + get_db; set current_user.telegram_chat_id = None; set current_user.telegram_link_token = None; set current_user.telegram_link_token_expires_at = None; await db.commit(); return Response(status_code=204)

    Extend backend/app/main.py:
    - Add "from app.routers import notifications" to the router import block (after discovery import, line ~16)
    - Add "application.include_router(notifications.router, prefix='/api', tags=['notifications'])" after the discovery include_router line (~line 84)

    Create backend/tests/test_notifications.py:
    - Module docstring listing 6 tests and NOTIF-04 / security refs (T-06-01 through T-06-06)
    - Imports: from __future__ import annotations; uuid; pytest; pytest_asyncio; unittest.mock (AsyncMock, patch); httpx (ASGITransport, AsyncClient); from app.main import app; from app.config import settings
    - WEBHOOK_SECRET constant = "test-webhook-secret-notif"
    - CHAT_ID constant = 77777
    - authed_client fixture (module-scope): registers user with unique email via POST /api/auth/register, mocking store_refresh_token; yields AsyncClient with auth cookie
    - test_create_link_token(authed_client): monkeypatch settings.telegram_bot_username = "tenderitbot"; POST /api/notifications/telegram/link-token; assert 200; assert "deep_link" in body; assert body["deep_link"].startswith("https://t.me/tenderitbot?start="); token = body["deep_link"].split("?start=")[1]; assert len(token) == 43
    - test_link_token_unauthenticated(): POST without cookie; assert 401
    - test_get_status_not_connected(authed_client): GET /api/notifications/status; assert 200; assert body["telegram_connected"] is False; assert body["telegram_chat_id"] is None
    - test_disconnect(authed_client): first call POST link-token to generate token; then DELETE /api/notifications/telegram; assert 204; then GET /api/notifications/status; assert telegram_connected is False (verifies disconnect cleared any leftover state)
    - test_status_unauthenticated(): GET /api/notifications/status without cookie; assert 401
    - test_disconnect_unauthenticated(): DELETE /api/notifications/telegram without cookie; assert 401

    Note on test_get_status_not_connected: after a fresh register, telegram_chat_id is NULL → telegram_connected must be False. The test verifies this initial state — no DB manipulation needed.
    Note on test_disconnect: The test verifies DELETE 204 and that the status endpoint reflects disconnected state. A separate test for "connected → disconnect → disconnected" can be done by patching current_user.telegram_chat_id if needed, or using a db_session to set it directly — executor chooses the simplest pattern that passes.

    Do NOT use fenced code blocks in any file written; all code goes directly in .py files via Write tool.
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -m pytest tests/test_notifications.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_notifications.py pass (6 tests, 0 failures)
    - notifications.py exists with router = APIRouter() and three route decorators: @router.post("/notifications/telegram/link-token"), @router.get("/notifications/status"), @router.delete("/notifications/telegram", status_code=204)
    - POST link-token uses secrets.token_urlsafe(32) — not uuid4() — confirmed by grep "token_urlsafe" backend/app/routers/notifications.py
    - POST link-token stores timezone-aware expires_at: uses datetime.now(timezone.utc), NOT datetime.utcnow()
    - DELETE endpoint sets current_user.telegram_chat_id = None AND current_user.telegram_link_token = None (both cleared per D-05)
    - GET status endpoint does NOT accept db=Depends(get_db) as dependency (not needed — user loaded by get_current_user)
    - main.py contains "from app.routers import notifications" and include_router call with prefix="/api" and tags=["notifications"]
    - curl -s -X POST http://localhost:8000/api/notifications/telegram/link-token (without cookie) returns 401
  </acceptance_criteria>
  <done>
    notifications.py exists with 3 JWT-gated endpoints; main.py registers the router;
    all test_notifications.py tests pass including unauthenticated 401 checks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend telegram_webhook.py with /start handler + add /start tests to test_notifications.py</name>
  <files>
    backend/app/routers/telegram_webhook.py,
    backend/tests/test_notifications.py
  </files>
  <read_first>
    - backend/app/routers/telegram_webhook.py — read the full file; understand lines 121–130 (secret guard + early return) and lines 227–230 (async with telegram.Bot pattern); _handle_start_command goes BEFORE the @router.post decorator
    - backend/tests/test_telegram_webhook.py — _message_update() helper and webhook_client fixture with app.state.arq_redis mock; override_settings fixture with monkeypatch.setattr(settings, "telegram_webhook_secret", ...)
    - backend/tests/test_notifications.py — file created in Task 2; append /start tests here
    - .planning/phases/06-notifications/06-PATTERNS.md — Pattern assignments for telegram_webhook.py, exact insertion point, _handle_start_command structure
    - .planning/phases/06-notifications/06-RESEARCH.md — Pattern 2 (exact /start extension code), Pitfall 1 (/start vs /start TOKEN), Pitfall 2 (timezone naive/aware), Pitfall 3 (context.args unavailable)
  </read_first>
  <behavior>
    - POST /api/telegram/webhook with valid secret + message update {"text": "/start VALID_TOKEN"} where user exists with that token and expires_at is future:
      → sets user.telegram_chat_id = message.from_user.id; sets user.telegram_link_token = None; returns {"ok": True}
    - POST /api/telegram/webhook with valid secret + message update {"text": "/start EXPIRED_TOKEN"} where token exists but expires_at < now:
      → does NOT set telegram_chat_id; sets telegram_link_token = None (cleanup); calls bot.send_message with text containing "Ссылка устарела"; returns {"ok": True}
    - POST /api/telegram/webhook with valid secret + message update {"text": "/start"} (plain /start, no token):
      → no DB changes; returns {"ok": True} (graceful ignore)
    - POST /api/telegram/webhook with valid secret + message update {"text": "Hello"} (non-command):
      → no DB changes; returns {"ok": True} (existing behavior preserved)
    - Existing callback_query tests in test_telegram_webhook.py continue to pass unchanged
  </behavior>
  <action>
    Extend backend/app/routers/telegram_webhook.py — minimal changes only:

    1. Add new imports at top of file (only if not already present): from datetime import timezone as dt_timezone (note: datetime and timezone are already imported on line 25 as "from datetime import datetime, timezone" — no new import needed); from sqlalchemy import select (already on line 29)

    2. Add async helper function _handle_start_command(message, db: AsyncSession) -> None ABOVE the @router.post("/telegram/webhook") decorator (before line 108). The function:
       - Extracts text: text = message.text or ""; if not text.startswith("/start "): return (guard against plain /start and non-/start messages — note the trailing space is MANDATORY)
       - Extracts token: token = text[7:].strip(); if not token: return
       - Extracts chat_id: chat_id = message.from_user.id if message.from_user else None; if not chat_id: return
       - Queries User by token: result = await db.execute(select(User).where(User.telegram_link_token == token)); user = result.scalar_one_or_none()
       - Opens bot context: async with telegram.Bot(settings.telegram_bot_token) as bot:
         - If user is None: await bot.send_message(chat_id=chat_id, text="Ссылка не найдена. Зайдите в TenderIt и получите новую."); return
         - Check expiry: now = datetime.now(timezone.utc); if user.telegram_link_token_expires_at and user.telegram_link_token_expires_at < now: reply "Ссылка устарела. Зайдите в TenderIt и получите новую ссылку"; clear token fields; await db.commit(); return
         - Success: user.telegram_chat_id = chat_id; user.telegram_link_token = None; user.telegram_link_token_expires_at = None; await db.commit(); await bot.send_message(chat_id=chat_id, text="Telegram успешно подключён к TenderIt ✓")

    3. Modify the if not update.callback_query block (lines 128–130): insert message branch BEFORE the return statement:
       - if update.message and update.message.text: await _handle_start_command(update.message, db)
       - The existing "return {"ok": True}" stays as-is after the new branch
       - DO NOT touch the callback_query branches (confirm:* or disc:* handlers)
       - DO NOT move or modify the X-Telegram-Bot-Api-Secret-Token guard at lines 121–123

    4. Add a docstring update to the _handle_start_command noting: "T-06-04: Runs after T-05-31 secret guard — all calls to this function are already authenticated by Telegram."

    Extend backend/tests/test_notifications.py (append new tests at the bottom):

    Add two helper builders:
    - _start_message_update(chat_id, text) → dict: builds a Telegram Update JSON with "message" containing the given text and from_user.id = chat_id, plus required date field (int(time.time()))
    - No changes to the authed_client fixture

    Add module-level fixture webhook_notif_client (function-scope): sets app.state.arq_redis = AsyncMock(); yields AsyncClient; teardown removes arq_redis from state — copy pattern from test_telegram_webhook.py's webhook_client fixture

    Add override_webhook_secret fixture (monkeypatch): sets settings.telegram_webhook_secret = WEBHOOK_SECRET

    Write test_webhook_start_links_telegram(webhook_notif_client, override_webhook_secret):
    - Create a mock user object with telegram_link_token = "testtoken123abc" and telegram_link_token_expires_at = datetime.now(timezone.utc) + timedelta(minutes=10) and telegram_chat_id = None
    - Patch app.routers.telegram_webhook._handle_start_command dependency: because the handler does a real DB lookup, patch "app.routers.telegram_webhook.telegram.Bot" as AsyncMock (use patch context manager) AND patch the select result via patching the db.execute call OR test via a simpler approach: patch _handle_start_command itself as AsyncMock and verify it was called with the right message text — executor chooses whichever is less fragile
    - Alternative (preferred if simpler): patch "app.routers.telegram_webhook._handle_start_command" as AsyncMock; post webhook with /start token message; assert _handle_start_command was called once; assert response == {"ok": True}
    - This verifies the routing/dispatch without requiring full DB setup for /start in the test suite

    Write test_webhook_plain_message_no_dispatch(webhook_notif_client, override_webhook_secret):
    - Post webhook with message text "Hello" (non-/start)
    - Patch _handle_start_command as AsyncMock
    - After call, verify _handle_start_command was NOT called (text does not start with "/start ")
    - Assert response == {"ok": True}

    Write test_webhook_plain_start_no_dispatch(webhook_notif_client, override_webhook_secret):
    - Post webhook with message text "/start" (plain /start, no token)
    - Patch _handle_start_command as AsyncMock
    - Assert _handle_start_command was called once BUT internally returned early (no chat_id set) — OR simply verify the handler was called (the handler's own guard prevents DB writes); assert response == {"ok": True}

    REQUIRED: Add test_expired_token_does_not_set_chat_id — verifying T-06-05 expired token behavior is mandatory (see VALIDATION.md 06-01-04). Approach: patch _handle_start_command with an AsyncMock that raises an AssertionError if chat_id is set when token is expired, OR use the db_session fixture to create a real User row with an expired link token and assert telegram_chat_id remains None after _handle_start_command is called directly. Executor chooses whichever approach is less fragile given the db_session fixture state, but the test is NOT optional.
  </action>
  <verify>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -m pytest tests/test_notifications.py -x -q 2>&1 | tail -20</automated>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -m pytest tests/test_telegram_webhook.py -x -q 2>&1 | tail -10</automated>
    <automated>cd /Users/smatov/GitLab/TenderIt/backend && python -m pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - telegram_webhook.py contains _handle_start_command function defined before @router.post
    - _handle_start_command uses text.startswith("/start ") (with trailing space) — confirmed by grep: grep "startswith.*\/start " backend/app/routers/telegram_webhook.py returns match
    - _handle_start_command does NOT use text.split()[1] or context.args — grep for "split\(\)\[1\]" returns no match in telegram_webhook.py
    - datetime comparison uses datetime.now(timezone.utc) — NOT datetime.utcnow() — grep "utcnow" returns no match in telegram_webhook.py
    - Lines 121–123 (X-Telegram-Bot-Api-Secret-Token guard) unchanged — confirmed by reading: if incoming_secret != settings.telegram_webhook_secret: raise HTTPException(status_code=403)
    - Existing test_telegram_webhook.py tests: all pass (including test_webhook_plain_message_returns_ok which still passes because "Hello" text does not start with "/start ")
    - New tests in test_notifications.py: test_webhook_start_links_telegram passes; full suite cd backend && python -m pytest -x -q exits 0
    - test_webhook_plain_message_no_dispatch: asserts _handle_start_command NOT called for non-/start text
  </acceptance_criteria>
  <done>
    _handle_start_command handles /start TOKEN: sets chat_id on valid token, rejects expired token, ignores plain /start.
    All test_notifications.py and test_telegram_webhook.py tests pass. Full backend suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI /api/notifications/* | JWT cookie auth; untrusted HTTP client sends link-token/status/disconnect requests |
| Telegram servers → FastAPI /api/telegram/webhook | Telegram bot secret token header; untrusted message payloads from Telegram |
| Telegram user → FastAPI (via /start TOKEN message) | Link token embedded in message.text; token must be validated before trusting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | POST /notifications/telegram/link-token | mitigate | `secrets.token_urlsafe(32)` = 256-bit entropy; 15-min expiry via `telegram_link_token_expires_at`; unique DB index prevents collision |
| T-06-02 | Elevation of Privilege | `_handle_start_command` token consumption | mitigate | Token cleared on first successful use (`telegram_link_token = None` + `await db.commit()`) — token cannot be replayed |
| T-06-03 | Elevation of Privilege | DELETE /notifications/telegram | mitigate | `get_current_user` dep derives user from JWT only; no user_id in request body; `current_user.telegram_chat_id` cleared — cannot affect other users |
| T-06-04 | Spoofing | POST /api/telegram/webhook (message update) | mitigate | T-05-31 `X-Telegram-Bot-Api-Secret-Token` guard at lines 121–123 preserved unchanged; `_handle_start_command` runs only AFTER this guard — inherits protection automatically |
| T-06-05 | Elevation of Privilege | `_handle_start_command` expiry check | mitigate | Compare `user.telegram_link_token_expires_at < datetime.now(timezone.utc)` — timezone-aware comparison prevents TypeErrors; expired token clears link fields without setting `telegram_chat_id` |
| T-06-06 | Information Disclosure | GET /notifications/status | accept | Returns `telegram_chat_id` to the authenticated user only (JWT-scoped); chat_id is a non-sensitive internal Telegram ID; no PII; low-value target |
</threat_model>

<verification>
Run after all tasks complete:

```
cd /Users/smatov/GitLab/TenderIt/backend && python -m pytest -x -q
```

Suite must be fully green before moving to Plan 06-02.

Check NOTIF-04 coverage:
- POST link-token → deep_link returned: test_create_link_token passes
- GET status: test_get_status_not_connected passes
- DELETE disconnect: test_disconnect passes
- Webhook /start dispatch: test_webhook_start_links_telegram passes
- Webhook secret guard preserved: test_webhook_rejects_wrong_secret still passes
</verification>

<success_criteria>
- `cd backend && python -m pytest -x -q` exits 0 with all existing + new tests passing
- notifications.py has 3 endpoints all using `Depends(get_current_user)` as first dependency
- POST link-token generates token via `secrets.token_urlsafe(32)` (43 chars, URL-safe base64)
- `_handle_start_command` in telegram_webhook.py: uses `/start ` startswith check (space mandatory), timezone-aware datetime comparison, clears token on success or expiry
- main.py include_router for notifications present
- No WhatsApp/Twilio code anywhere (D-01 honored)
- No `telegram_link_token_expires_at` comparison using `datetime.utcnow()` (naive) — always `datetime.now(timezone.utc)`
</success_criteria>

<output>
After completion, create .planning/phases/06-notifications/06-01-SUMMARY.md
</output>
